# 报告书三模块第一版初稿

> 项目：基于机器人与 DNA-PCR 技术的孢子病害识别关键技术研究  
> 版本：v1.0  
> 日期：2026-09-06  
> 范围：SLAM + RTK + 路径规划总方案；总控制台 + 电脑/手机双端控制；孢子扩散预测预警算法  
> 说明：本文为正式报告正文初稿。已有事实直接陈述；已派发但尚未返回的成果使用 H-01～D-04 占位，提交前据实回填。

---

# 一、SLAM + RTK + 路径规划总方案

## 1.1 应用需求与总体目标

农业病害巡检机器人需要在垄间通道、地头转弯区和临时搭建的模拟农田中持续完成建图、定位、路线跟踪和定点采样。与室内移动机器人相比，农田环境具有地面不平整、重复纹理多、作物遮挡明显、卫星信号和无线网络质量波动等特点。单独依赖轮速里程计会随行驶距离积累误差，单独依赖二维激光 SLAM 难以获得稳定的地理绝对坐标，而普通单点 GNSS 的米级误差也难以满足采样位置精确绑定和重复巡检的需要。因此，本项目提出由局部连续定位、全局绝对定位和任务级路径规划共同构成的自主巡检导航方案。

本方案的设计目标包括：建立统一、无冲突的机器人坐标体系；利用轮速、IMU 与二维激光 SLAM 获得连续局部位姿；利用北斗 RTK 提供全局绝对位置约束；根据田块边界和垄向自动生成牛耕式覆盖路线；通过路径跟踪和任务状态机完成巡航、转弯、到点停车、采样驻留和返航；在位姿失效、雷达过期、前方障碍或通信中断时进入安全停止或降级状态。定位误差小于 0.5 m 是本方案的目标指标，其最终实测结果由 H-01 和 A-01 任务回填，不在初稿阶段预设结论。

【图 N-1 占位：SLAM + RTK + 路径规划总体方案图】

## 1.2 系统组成与分层架构

车辆平台采用 WHEELTEC R550 PLUS 四轮底盘，C50C STM32 下位机负责电机驱动、编码器采集和底盘状态反馈，树莓派 5 作为车载计算平台。传感器层包括轮速编码器、ICM20948 IMU、YDLIDAR X3 Pro 二维激光雷达以及北斗 RTK 接收机。软件运行于 ROS 2 Humble 环境，由底盘驱动、雷达驱动、局部融合定位、SLAM、RTK 数据处理、路线解析、路径跟踪、任务状态机和安全判定等节点组成。

系统分为三层。感知与执行层负责采集轮速、惯性、激光和卫星定位数据，并执行速度控制命令；定位与导航层负责局部状态估计、地图构建、全局坐标约束、路线变换和车辆跟踪；任务层负责任务文件校验、采样点管理、状态发布、异常处理和结果记录。各层通过标准 ROS 2 话题和明确定义的坐标变换连接，使传感器或算法可以独立替换，降低整机集成风险。

现有原型已经完成底盘、激光雷达、EKF 和 SLAM Toolbox 的组合运行，融合里程计发布频率约为 20 Hz，激光雷达约为 11.3～11.45 Hz。上述结果证明基础数据链和软件部署方式可行，但并不等同于 RTK 融合精度已经达到目标。

## 1.3 坐标体系与数据一致性

为避免网页规划、SLAM 地图和车辆控制之间产生方向、原点或单位歧义，本方案统一采用 `field`、`map`、`odom`、`base_footprint`、`base_link` 和 `laser_link` 等坐标系。`field` 表示田块局部 ENU 坐标，单位为米；`map` 表示 SLAM 建立的局部地图；`odom` 表示连续但可能缓慢漂移的局部里程计坐标；`base_footprint` 与 `base_link` 描述车辆平面运动和车体安装位置；`laser_link`、`imu_link` 与 `gnss_link` 分别描述传感器外参。

TF 链遵循唯一发布者原则：局部 EKF 发布 `odom→base_footprint`，SLAM Toolbox 发布 `map→odom`，车体模型发布 `base_footprint→base_link→sensor_link`。RTK 融合节点不再发布第二条相互竞争的 `map→odom`，而是利用经纬度转换后的 ENU 观测估计 `field` 与 `map` 的全局对齐关系，或将合格的绝对位置约束送入统一融合模块。该设计避免多个节点争夺同一 TF 变换，同时兼顾局部连续性与全局可追溯性。

所有路线、采样和检测数据统一使用 `field_id`、`route_id`、`mission_id`、`sample_id` 和时间戳关联。路线文件固定采用 `route_v1.json`，声明 `schema_version: "1.0"`、`frame_id: "field"`、`coordinate_type: "local_enu"` 和 `unit: "m"`。由此保证路线生成端和车辆执行端对坐标、单位及采样身份具有一致理解。

【图 N-2 占位：field/map/odom/base_link 坐标系与 TF 关系】

## 1.4 SLAM 与局部连续定位

局部定位采用轮速—IMU EKF 与二维激光 SLAM 分层协同。底盘驱动根据编码器反馈计算线速度、角速度和轮式里程计；IMU 提供角速度和线加速度；`robot_localization` 以扩展卡尔曼滤波方法融合二者，输出连续的 `/odometry/filtered`。路线跟踪优先使用融合里程计，在融合结果超过新鲜度阈值时回退到原始 `/odom`，若所有位姿均过期则立即输出零速度。

YDLIDAR X3 Pro 发布 `/scan`，SLAM Toolbox 通过激光扫描匹配、里程计先验和回环检测构建二维占据栅格地图。项目已在 8 m × 6 m、三条模拟垄、1.20 m 净通道和 1.60 m 地头的 canonical 仿真场景中完成无界面启动、九航点低速闭环、地图保存和位姿图保存。两次同配置仿真地图的垄中心最大偏移为 0.050 m，低于项目设定的 0.150 m 仿真重复性门槛。该结果用于证明场景、启动链、建图链和保存链可运行，不将其解释为实车定位误差。

2026-09-01 的临时单垄场地测试进一步完成了地图文件保存和 U 形掉头，去程约 3.36 m，记录的航向漂移约 0.2°。H-02 任务将在提交前补充模拟农田实物场景搭建、标准地图文件和现场素材。

【图 N-3 占位：仿真地图、标准农田实物地图及建图流程】  
【H-02 待回填：场景尺寸、现场照片、地图文件名、分辨率、建图轨迹和验收说明】

## 1.5 北斗 RTK 与 SLAM 融合定位

普通 GNSS 可以为机器人提供大致经纬度，但其单点误差和状态信息不足以承担高精度重复巡检。项目现有 ATGM336H 适配包已经实现 NMEA GGA/GLL/VTG 解析，可发布 `/fix`、速度、航向、原始报文、诊断和可选 ENU 坐标；该模块只作为普通 `single` 定位和软件接口验证基础，不作为 RTK 精度证据。

完整方案采用支持北斗多频 RTK 的移动站，配合测量型天线和 CORS/NTRIP 差分服务；当网络条件不具备时，可切换为自建基站。RTK 节点除经纬高外，还应输出 Single、Float、Fix 状态、卫星数、水平精度因子、协方差和差分龄期。经纬度通过现场测定的原点转换为局部 ENU 坐标，并结合天线相位中心到 `base_link` 的杆臂外参进行修正。

融合算法采用“局部连续估计 + 全局质量约束”的结构。轮速、IMU 与 SLAM 维持高频、平滑的局部轨迹；RTK 作为低频绝对位置观测，用于校正长期漂移和建立地理坐标。融合节点根据 RTK 解状态和协方差动态设置权重：Fix 状态在创新量和差分龄期满足阈值时提供强约束，Float 状态降低权重，Single 或数据过期时仅保留状态提示而不强制修正。对于突跳点，可采用创新残差门限或马氏距离进行拒绝；RTK 恢复后通过渐进校正避免全局坐标突变。

本方案的优势在于不把 RTK 和 SLAM视为互相替代的定位源。SLAM解决短时连续性和局部障碍几何，RTK解决地图与地理空间之间的绝对约束。当卫星信号短时受遮挡时，机器人仍可依靠局部融合继续低速运行或安全停车；当 SLAM 纹理退化时，RTK 可限制全局漂移。A-01 将提供融合算法源码、ROS 节点工程和定位轨迹对比图，H-01 将提供硬件系统整合和融合定位测试报告。

【图 N-4 占位：RTK/SLAM 分层融合算法流程】  
【A-01 待回填：节点名称、输入输出话题、状态向量、质量门参数、轨迹对比图】  
【H-01 待回填：RTK 型号、差分方式、测试场地、Fix 比例、参考真值、RMSE/P95/最大误差】

## 1.6 田块覆盖路径规划

路径规划以田块边界、垄向、垄间距、地头宽度、采样密度和车辆最小转弯半径为输入。操作人员可在 Web 控制台导入底图或 SLAM 地图，标定边界和比例尺，系统首先校验多边形闭合、重复点、自交和面积等几何条件，再根据主垄方向生成等距平行作业线。相邻作业线采用方向交替的牛耕式顺序连接，从而减少空驶距离；地头区域采用满足最小转弯半径的 U 形过渡，采样点则在边界内根据覆盖需求和风险权重分配。

路线生成后统一转换为米制 local ENU 坐标。每个航点包含序号、`x_m`、`y_m`、`yaw_rad`、类型、轮次、`sample_id`、速度上限和到达容差。导出前检查航点连续性、边界约束、速度上限、曲率、采样身份和起终点完整性。车辆接收路线后，将 `field` 航点变换到当前 `map` 坐标，并进行等弧长重采样和 Chaikin 平滑，减少尖角对四轮底盘的冲击。

该方案的创新不在于单独使用牛耕式覆盖，而在于将规划坐标、任务身份、采样语义和车辆运动约束共同写入版本化任务包，使地图端生成的结果能够被车端直接验证和执行，也便于后续将风险热区转化为重点巡检任务。

【图 N-5 占位：田块边界、牛耕路线、地头转弯和采样点示意】

## 1.7 路径跟踪、任务状态机与安全门

车辆侧采用 Pure Pursuit 作为当前低速跟踪算法。控制器从平滑路线中寻找前视点，根据车辆位姿和前视点几何关系计算曲率，再生成线速度和角速度。当前配置的最大线速度为 0.12 m/s、最大角速度为 0.30 rad/s、前视距离为 0.5 m，控制频率为 10 Hz。低速和较长前视距离有利于提高当前验证阶段的稳定性；后续可根据通道宽度和定位质量进行自适应调整。

任务状态机覆盖任务载入、导航、到达、稳定停车、采样请求、采样执行、采样完成、继续行驶和任务完成，并支持暂停、恢复、返航、取消、停止和故障。真实采样器尚未接入时，以 2 s 模拟驻留验证任务时序；采样器接入后只需替换请求/完成接口，无需重写路线跟踪逻辑。

安全门持续检查位姿新鲜度、雷达新鲜度、路线合法性、任务状态和前方障碍。现有配置在前方 ±90° 扇区内取最近距离，阈值为 0.5 m；D-04 演示将重点展示前方 ±45° 核心危险区内的刹停或绕障效果，最终报告应记录视频对应的实际运行参数。当检测到障碍、扫描过期、位姿过期或未知状态时，控制器输出零速度并发布异常状态。浏览器中的软件停车不能替代物理急停，现场运动始终需要安全员和硬件断电手段。

项目现有小尺度实车记录已覆盖 1 m 直线、3 m 直线、方形回环、双垄往复、完整多点采样路线和静态障碍停车，共 6 类 9 次。D-03 将补充牛耕式转弯、巡航和全覆盖视频，D-04 将补充安全门避障视频。固件层失联停车曾记录 1.309 s 和 1.409 s，超过项目 1.2 s 门槛，因此方案采用 ROS 控制超时、车辆任务安全门、人工物理急停三层防护，并将固件优化作为工程化改进项。

【图 N-6 占位：任务状态机】  
【D-03 待回填：视频文件、路线长度、速度、覆盖率、转弯和巡航关键帧】  
【D-04 待回填：视频文件、扇区、阈值、刹停距离、绕障过程和现场安全条件】

## 1.8 方案创新性与可行性

本模块的创新性主要体现在三方面。第一，采用局部连续定位与全局绝对约束分层融合，在不破坏 ROS TF 唯一发布者原则的前提下兼顾 SLAM 连续性和 RTK 地理可追溯性。第二，以版本化米制路线任务包统一规划端和车辆端，将坐标、速度、转弯、采样身份和任务状态同时纳入导航流程。第三，将定位质量、传感器新鲜度和障碍信息引入任务状态机，使机器人在数据退化时具有可解释的停车和降级行为。

方案具备较好的实施基础：现有底盘、雷达、EKF、SLAM 和路线跟踪代码均已运行或通过测试；仿真地图、临时单垄地图和小尺度路径试验为后续整合提供了基础；RTK、模拟农田实物场景、融合节点和演示视频已有明确任务及交付物。企业落地时可根据场地规模选择 CORS 或基站方案，并通过替换 RTK 接收机、调整速度和扩大地头空间适应不同作物与农田条件。

本阶段以证明技术路线完整和原型可行作为主要目标。正式产品仍需开展更长时间的户外雷达测试、RTK 固定解覆盖统计、独立真值误差评估、正式小花园重复路线试验、真实采样器联动和安全认证。

【表 N-1 占位：设计指标、已有基础、H/A/D 任务成果和工程化验证四列对照】

---

# 二、总控制台 + 电脑/手机双端控制

## 2.1 建设目标与应用场景

农田病害监测涉及地块地图、机器人状态、环境参数、孢子粗测、PCR 检测、病害记录、预测结果和处置告警等多类信息。传统做法往往依赖多个独立软件和人工文件传递，难以保证任务、样本、位置和预测结果的一致性，也不利于企业进行多地块管理和现场快速响应。因此，本项目设计统一总控制台，将规划、检测、分析、预测和机器人监控纳入同一业务界面，并通过桌面端与移动端应用封装适配不同使用场景。

电脑端主要面向任务规划、地图编辑、批量数据分析、多模型对比和报告导出；手机端主要面向现场巡检、设备状态查看、告警确认、任务暂停/返航和快速处置。两端共享同一业务核心和数据契约，避免维护两套逻辑不一致的软件。D-01 任务将在提交前补充 Windows/Linux 安装包、Android APK 和运行截图。

【图 C-1 占位：电脑端和移动端应用场景】

## 2.2 控制台总体架构

控制台采用“一套响应式 Web 核心 + 多端封装”的总体路线。表现与封装层基于 Next.js、React 和响应式 CSS，桌面端与 Android 端复用相同页面、组件和业务逻辑；具体封装工具、版本和安装包格式以 D-01 最终交付为准。业务层包括路径规划、环境感知、PCR 分析、病害识别、趋势预警、监管告警和机器人监控七个页签。数据与契约层负责统一路线、任务、遥测、样本、预测和会话格式。设备接入层通过 rosbridge WebSocket 接收 ROS 2 数据，并保留 Mock、Replay 和后续 HTTP 服务等来源。

七个页签各自承担清晰职责：路径规划页负责底图或 SLAM 地图导入、边界标定和路线导出；环境感知页负责温湿度等数据和阈值告警；PCR 分析页负责 Ct 值、浓度、质量和融合；病害识别页负责八类病害原型和人工确认；趋势预警页负责 3/5/7 天扩散与侵染风险；监管页负责地块、告警和处置记录；机器人监控页负责位姿、轨迹、雷达、电池、诊断和任务状态。

这种架构使总控制台既能作为现场原型的本地应用运行，也能在企业化阶段扩展为服务端部署。工程化版本可增加任务与数据服务、PostgreSQL/PostGIS、文件存储、用户权限和审计日志，而不改变前端业务模型和车辆接口。

【图 C-2 占位：控制台表现层、业务层、数据层、设备接入层架构】

## 2.3 电脑端与手机端功能设计

双端设计遵循“同平台、分重点”的原则。电脑端屏幕空间较大，适合显示完整地图、路线参数、PCR 表格、扩散热力图和多模型对比结果，因此承担地块建档、精细规划、批量分析、任务创建和报告生成。移动端强调单手操作和现场可读性，首页优先呈现连接状态、任务进度、RTK 质量、电池、告警和当前位置，并将任务暂停、取消、返航等操作设计为受限按钮和二次确认。

当前 Web 已设置 900 px、680 px 和 520 px 三档响应式规则。窄屏下，页签改为横向滚动，统计卡片和输入组件重排为两列或单列，避免关键按钮超出屏幕。D-01 完成后，应在 Windows、Linux 和 Android 真机上验证安装、启动、页面适配和同一数据源的一致显示。

出于安全考虑，直接线速度和角速度控制只保留在现场维护或调试模式，并默认锁定。手机端面向生产使用时原则上只提供高层任务操作和告警处置，不承担持续手动驾驶。任何远程控制均不能替代车旁物理急停和现场安全确认。

【图 C-3 占位：电脑端全功能界面】  
【图 C-4 占位：Android 移动端任务与告警界面】  
【D-01 待回填：封装框架、Windows/Linux 安装包、APK、版本号、安装和运行截图】

## 2.4 数据契约与实时接入

控制台通过版本化契约连接规划结果、机器人任务和检测数据。路线使用 `route_v1.json`；任务和遥测分别记录 `mission_id`、状态、当前航点、完成比例、预计剩余时间、位姿、雷达、电池、诊断、RTK 和采样器状态。样本使用 `sample_id` 与任务、坐标、时间、PCR 和风险结果关联。未知 schema、坐标系、单位或状态将被拒绝，避免错误数据在系统中静默传播。

真实车辆数据由 rosbridge 转换为浏览器可使用的 WebSocket 消息。控制台优先订阅 `/odometry/filtered`，在其不可用时回退 `/odom`，同时接收 `/scan`、`/battery_state`、`/diagnostics`、`/mission/status` 和可选 `/gps/status`。适配层把车辆原生状态转换为统一任务状态，并根据接收时间判断位姿、雷达和诊断是否过期。失联或消息格式异常时，界面显示 stale 或 unavailable，而不是沿用旧数据伪装成正常。

为了支持没有实机时的开发和展示，系统提供 Mock、Replay 和 rosbridge 三种来源。Mock 用确定性任务模拟器生成行驶、到达、采样和返航过程，并可注入断联、位姿跳变、RTK 降级、低电量、障碍和采样失败；Replay 用版本化记录复现既有任务；rosbridge 用于真实遥测。每种数据都带来源标签，报告和导出文件能够区分模拟、回放和实测数据。

【图 C-5 占位：ROS 2—rosbridge—统一契约—双端应用数据流】

## 2.5 控制权限与安全机制

控制台的安全策略建立在显式连接、默认锁定、分级权限和操作审计基础上。页面加载时不自动连接机器人；用户需主动填写或确认 rosbridge 地址并发起连接。运动控制默认锁定，解锁前提示确认车辆状态、区域清空和急停人员就位；断开连接后系统自动重新锁定。Mock 和 Replay 模式不发布真实运动指令。

面向企业部署，建议设置观察员、农技人员、任务管理员、现场安全员和系统管理员五类角色。观察员仅查看地图和状态；农技人员确认 PCR、病害和处置建议；任务管理员创建路线和管理任务；现场安全员负责运动解锁和物理急停；系统管理员管理设备、账户、数据源和审计。当前本地原型尚未部署完整账户和服务端 RBAC，因此该部分作为工程化设计，不写成已上线功能。

手机端的任务暂停、返航和取消应采用二次确认，并与车端状态机协同；直接速度控制应禁用或仅在本地维护模式开放。发生通信中断时，浏览器立即将连接状态标记为异常，车端则依靠自身看门狗和安全门停车，避免把安全责任放在网络和浏览器上。

【图 C-6 占位：连接、授权、任务操作、异常锁定和物理急停的安全流程】

## 2.6 本地存储与企业化扩展

当前控制台通过版本化本地会话仓库保存地块、路线、检测、预测和告警状态，支持旧版本迁移、导入导出及敏感字段拒绝。该方式便于离线演示和单机运行，但不适合多人协作、跨设备同步和长期数据治理。

企业化部署可增加独立任务与数据服务，通过 HTTPS 和 WebSocket 向双端应用提供统一 API；空间数据存入 PostgreSQL/PostGIS，地图、图像、视频和报告存入文件或对象存储；用户身份、操作记录和设备状态进入审计系统。边缘侧仍保留本地缓存和离线任务，使弱网络条件下车辆不会因云端不可达而失去基本运行能力。网络恢复后再按任务 ID 和时间戳同步数据。

该设计允许项目从当前本地原型平滑扩展到多地块、多设备和多用户平台，不要求在比赛阶段一次性实现全部云端功能，也能够向企业清楚说明后续建设边界和实施顺序。

## 2.7 方案创新性与可行性

控制台模块的第一项创新是以统一 Web 业务核心支撑电脑和移动端封装，使两端共享算法、数据契约和界面组件，降低多端开发与版本维护成本。第二项创新是将路径规划、车辆遥测、PCR、预测和告警纳入同一数据模型，以任务和样本 ID 形成可追溯管理。第三项创新是同时提供 Mock、Replay 和实时数据源，并对数据来源和新鲜度进行显式标记，使系统既便于离线验证，也避免将模拟信息误作真实结果。

当前七页签、路线导出、机器人监控、响应式布局、Mock/Replay 和 rosbridge 适配已经具备代码基础，Web 静态检查、类型检查、构建以及相关测试均已通过，静止状态下的真实 rosbridge 数据接收已有记录。D-01 将补充多端应用封装和安装展示。由此，报告可合理论证多端总控制台的可行性，同时将账户体系、云端数据库、高清视频和产品级远程控制列为工程化扩展。

【表 C-1 占位：电脑端、手机端功能与权限对照】  
【表 C-2 占位：Windows/Linux/Android 安装与核心功能验证结果】

---

# 三、孢子扩散预测预警算法

## 3.1 问题背景与算法目标

传统病害监测通常在作物出现症状后开展人工巡查，难以利用空气中孢子和环境条件提前判断风险。孢子传播又受到风速、风向、温湿度、光照、降雨、冠层结构和病原再释放等多因素影响。传统稳态高斯烟羽模型具有计算快、参数少和可解释性强等优点，但通常假设连续单点源、恒定风场和平稳环境，难以表达农田中风向转折、间歇释放、降雨清除和多热点初始场。

本项目设计 PCR 观测驱动的动态孢子扩散预测方法。算法先融合粗测和 PCR 观测形成空间基准场，再通过粒子滤波估计疑似源位置和相对源强，以逐小时风场驱动平流—扩散—沉降—失活—再释放过程，最后结合病害侵染窗口和剂量—响应关系输出 3、5、7 天风险分布。算法目标是提高复杂动态场景下的空间表达能力，并为重点复检和精准防控提供依据，而不是在所有场景中无条件替代高斯烟羽模型。

【图 A-1 占位：PCR 观测同化—动态扩散—侵染预警总体流程】

## 3.2 输入、输出与数据质量控制

算法输入包括田块边界、统一坐标下的采样点、粗测浓度或风险、PCR Ct 值及换算浓度、样本质量、风速和风向序列、温度、湿度、光照、降雨、作物冠层参数、病情指数以及目标病害类型。每条观测必须携带 `sample_id`、坐标、采样时间和数据来源，避免把不同轮次或不同位置的结果错误合并。

算法输出包括疑似源位置、相对源强、后验不确定度、3/5/7 天浓度场、峰值、P90、均值、超阈值区域、侵染概率、潜育期提示、风险等级和处置建议。所有输出记录模型版本、参数版本和数据来源，以便结果追溯。

PCR 浓度的物理含义依赖采样空气体积、洗脱体积、稀释倍数、捕获效率和提取回收率。在该单位链完全冻结前，模型结果统一表述为“相对浓度”或“合成浓度”，不直接宣称为真实 `copies/m³`。pending 或 invalid 样本不参与源估计，doubtful 样本可以保留但降低权重。

## 3.3 传统高斯烟羽基线

高斯烟羽模型以单一连续点源为基础，将下风向浓度描述为源强、平均风速、横向扩散尺度、垂向扩散尺度和源高的函数。其基本形式可写为：

\[
C(x,y,z)=\frac{Q}{2\pi u\sigma_y\sigma_z}
\exp\left(-\frac{y^2}{2\sigma_y^2}\right)
\left[\exp\left(-\frac{(z-H)^2}{2\sigma_z^2}\right)+
\exp\left(-\frac{(z+H)^2}{2\sigma_z^2}\right)\right]
\]

其中，`Q` 为源强，`u` 为平均风速，`σ_y` 与 `σ_z` 由稳定度和下风距离决定，`H` 为有效源高。项目基线还加入受体高度、镜像源和沉降衰减。该模型适用于风场稳定、地形相对均一和连续释放条件，可作为方案公平比较的基准。

其局限在于浓度场沿单一平均风向展开，不能记忆历史风向和释放变化；降雨、紫外和再释放通常只能通过整体修正系数表达；当 PCR 观测呈现多个高值区时，单点源稳态假设可能不足。因此，本项目保留高斯烟羽作为稳态场景基线和粒子滤波中的快速前向近似，而不是将其完全弃用。

【图 A-2 占位：高斯烟羽与动态重构算法的假设和流程对比】

## 3.4 粗测—PCR 融合基准场

粗测数据具有覆盖范围大、采集速度快的优势，但受环境和传感器噪声影响较大；PCR 定量更具特异性，但采样点稀疏且检测成本较高。算法首先对粗测数据进行空间插值，获得连续趋势场；随后以 PCR 结果对趋势场进行稳健标定和局部残差校正。质量可靠的 PCR 点具有较高权重，存疑点增加不确定度，无效点被剔除。

融合后的基准场既保留粗测的空间覆盖，又利用 PCR 提高关键点的可信度。它既作为扩散模型的初始浓度场，也为疑似源反演提供观测。为避免空间外推失真，田块外网格被屏蔽，观测过少或空间分布退化时系统降低源估计置信度，并回退到不含源播种的融合场预测。

## 3.5 贝叶斯粒子滤波源项估计

疑似源状态定义为 `θ=[x_s,y_s,log10Q]`，分别表示源坐标和对数相对源强。粒子先验在田块有效区域和合理源强范围内生成，利用高斯烟羽快速计算每个粒子在观测点处的预测值。在 `log10` 空间中，根据预测值与 PCR 观测之差计算似然并归一化权重，得到源位置、源强、协方差和有效粒子数。

采用对数空间的原因是孢子浓度可能跨越多个数量级，并且 PCR 定量误差更适合按倍数解释。默认观测噪声可设为约 0.3 个数量级，存疑样本增加方差。当有效空间观测少于两个时，单个观测无法唯一确定源方向和距离，算法跳过源估计；两至三个观测时输出低置信度；观测增加后再给出更集中的不确定椭圆。

该方法将巡检数据从被动展示转化为可更新的源项后验。随着新增采样点进入系统，可重新计算疑似源和不确定度，为下一轮重点巡检提供位置依据。报告中使用“疑似源估计”而非“精确确定病原源”，以符合逆问题的不确定性特征。

【图 A-3 占位：粒子分布、源位置后验和不确定椭圆】

## 3.6 动态平流—扩散与孢子命运过程

重构算法采用逐时离散的二维 Eulerian 网格描述孢子浓度变化，其连续形式可概括为：

\[
\frac{\partial C}{\partial t}+\mathbf{u}\cdot\nabla C
=\nabla\cdot(K\nabla C)+S-
(\lambda_{uv}+\lambda_{dep}+\lambda_{rain})C+R
\]

其中，`C` 为网格浓度，`u` 为近冠层有效风矢量，`K` 为湍流扩散系数，`S` 为估计源播种，`λ_uv`、`λ_dep`、`λ_rain` 分别表示紫外失活、沉降和雨洗损失，`R` 表示病斑或冠层中的再释放。算法每小时读取风速和风向，计算平流位移；随后进行邻域扩散、损失衰减和局部再释放，并在 72、120 和 168 h 保存预测切片。

冠层有效风速由外部风速、株高和 LAI 进行衰减修正；温湿度影响孢子存活和病原活性；紫外强度增加失活；降雨造成短时雨洗；病情指数和冠层条件影响再释放。A-02 重构任务将进一步修正水平平流和沉降的量纲关系，引入逐时温湿度、光照和降雨序列，并提供计算效率与预测精度分析。

为了保持工程可实现性，Web 端采用粗网格逐时计算，复杂场景和报告对比由 Python 离线管线批量运行。两端共享场景、参数和输出 schema，并通过固定小场景快照检查结果趋势，避免离线图表与实际 Web 算法完全脱节。

【A-02 待回填：最终离散公式、逐时天气结构、参数版本、运行耗时、内存和精度分析】

## 3.7 侵染窗口与预警输出

孢子浓度反映传播暴露，不等同于作物一定发病。为使输出更接近农业决策，系统为八种目标病害配置适宜温度、湿度阈值、叶面湿润时长和潜育期。只有环境满足侵染窗口时，才根据累计孢子剂量计算侵染概率。剂量—响应关系可写为：

\[
P_{inf}=I_{window}\left(1-\exp(-D/D_0)\right)
\]

其中，`I_window` 表示侵染窗口是否满足，`D` 为预测期累计剂量，`D_0` 为需要由病害资料或实验标定的尺度参数。当前原型具有默认参数表和文献来源，正式应用时应针对作物、病原和采样单位重新标定。

系统分别输出 3、5、7 天浓度和侵染风险热力图，并结合峰值、P90、超阈值面积和置信度生成低、中、高风险提示。预警结果可用于建议增加下风向采样、缩短复检周期或对重点区域采取精准防控。没有真实未来发病标签时，报告只将其称为“风险预测与预警方案”，不将输出概率解释为真实田间准确率。

## 3.8 对比实验与阶段性结果

为评价动态模型相对传统高斯烟羽的适用范围，实验设置稳态高斯烟羽 G0、无源同化动态模型 E0、源同化但恒定风模型 E1 和完整时序模型 E2。比较指标包括留出点 `log10` RMSE、MAE、空间相关系数、热点区域 IoU、峰值位置误差、源位置误差和运行时间。训练点只用于浓度尺度标定和源估计，测试点不参与拟合。

现有第一轮合成实验使用独立动态高斯烟团集合生成参考场，每个场景设置 20 个校准点和 60 个留出测试点。稳定风场中，高斯烟羽 RMSE 为 0.437，低于重构方法的 0.720，说明基线在其适用条件内仍有优势；风向转折场景中，重构方法 RMSE 为 1.476，高斯烟羽为 3.497；风向变化与阵雨场景中，重构方法为 1.267，高斯烟羽为 3.200。动态两场景的 RMSE 分别降低约 57.8% 和 60.4%。这些结果说明重构方法的价值主要体现在非稳态复杂环境中的传播带和热点恢复能力。

上述数据属于固定随机种子的合成数值实验，不是真实农田观测。D-02 将补充新算法与高斯烟羽的正式对比图表和指标表；提交前还应根据 A-02 最终算法版本完成多随机种子、参数敏感性和必要消融测试。

【图 A-4 占位：稳态、风向转折、阵雨场景统一色标热力图】  
【图 A-5 占位：留出点 RMSE、相关性和热点 IoU 对比】  
【D-02 待回填：最终场景、原始 CSV/JSON、对比图、指标表和生成脚本】

## 3.9 方案创新性与可行性

算法模块的第一项创新是通过 PCR 空间观测反演疑似源位置和相对源强，并显式输出不确定性，使巡检数据能够持续修正预测初始条件。第二项创新是从单一稳态烟羽扩展到时序风场驱动的平流—扩散—损失—再释放模型，重点解决风向转折、降雨和间歇释放等非稳态问题。第三项创新是将孢子暴露与病害侵染窗口、剂量响应和潜育期结合，使输出从浓度图进一步转化为面向防控的时间和空间风险。

方案具有明确的软件基础。TypeScript 引擎已完成源项估计、动态预报、侵染风险和高斯 A/B 对照，相关引擎测试 16 项通过；Python 管线可生成可复现实验数据、指标和报告图。初步合成实验同时保留高斯模型胜出的稳态场景和动态模型胜出的复杂场景，避免通过单方面设计场景制造结果。A-02 和 D-02 已有明确的重构、效率、精度和展示任务，可以在提交前进一步补强证据。

面向企业应用，算法参数应根据区域作物、病害和气象数据进行本地化标定；模型应保留版本、参数、数据质量和置信度；预警应与农技人员确认和复检机制结合。当前阶段重点证明方法完整、可复现且具有动态场景优势，长期田间预测准确率和真实提前量则需要多轮跨时间观测进一步验证。

【表 A-1 占位：高斯基线与重构算法的假设、输入、过程和输出对比】  
【表 A-2 占位：已有合成结果、A-02/D-02 预期成果和后续田间验证】

---

# 四、初稿统一说明

以上三部分分别作为独立模块撰写，初稿阶段不强行构成连续技术链。各模块只说明自身输入、输出和可对接位置，后续由总报告整合阶段决定章节位置和过渡关系。

本文中的 H-01、H-02、A-01、A-02、D-01、D-02、D-03、D-04 均为已派发且预计在报告提交前完成的任务。初稿已经按照成果最终可回填的结构建立完整方案，但所有尚未获得的实测数值、源码版本、安装包、图片和视频均保留任务编号占位。只有收到原始证据并完成核验后，才可将“本方案设计”“预计形成”改写为“已完成”“实测表明”。

