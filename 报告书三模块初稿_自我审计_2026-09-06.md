# 报告书三模块第一版初稿自我审计

> 审计对象：`报告书三模块初稿_v1_2026-09-06.md`  
> 审计日期：2026-09-06  
> 审计目的：核查第一版是否满足用户要求、总基线和项目事实，并形成润色版修改清单

## 1. 审计结论

第一版已经具备三份独立模块正文的基本形态，能够作为报告初稿继续推进；它没有强行将三部分写成单一技术链，也没有把 H-01～D-04 的预期交付提前写成已完成事实。三部分均包含企业需求、总体方案、关键方法、创新性、可行性和结果占位，方向与总基线一致。

但第一版仍有四类需要改进的问题：

1. 部分段落仍偏“项目状态说明”，设计方案的实施步骤、工程维护和企业价值可以更集中；
2. 导航部分技术内容较完整，但 RTK 全局约束与 SLAM TF 的关系需要用更精确的语言避免歧义；
3. 控制台部分个别句子容易让读者误以为账户权限、审计和双端任务控制已经实现，应进一步区分当前实现与企业化设计；
4. 扩散算法部分需要更清楚地区分“当前 v1 合成代理结果”“A-02 重构后的正式模型”和“真实田间预警验证”。

因此，审计判断为：**要求总体满足，但仍需按下述清单润色后再作为交付初稿。**

## 2. 用户要求逐项核查

| 用户要求 | 第一版证据 | 判定 | 后续处理 |
|---|---|---|---|
| 生成三部分内容初稿 | 已形成导航、控制台、扩散算法三个一级章节 | 满足 | 保留独立章节结构 |
| 不强行构成单一技术链 | 三部分分别论证，仅在末尾说明输入输出接口 | 满足 | 润色版继续避免跨章节重复 |
| 聚焦完整可行设计方案 | 每部分包含总体方案、关键机制、创新性和可行性 | 基本满足 | 增加分阶段实施和企业落地语言 |
| 不把完整产品作为唯一评价标准 | 多处说明当前阶段以方案和原型可行为主 | 满足 | 减少“未完成项”带来的消极语气 |
| 可以按已派发任务完成为前提写完整逻辑 | H-01～D-04 均进入相应章节和占位 | 满足 | 进一步统一占位格式 |
| 不捏造事实 | RTK 精度、安装包、视频和最终算法指标均未提前宣称完成 | 满足 | 继续保留证据门槛 |
| 暂时只考虑文字，真实数据和图表可占位 | 使用文字正文、公式和图表占位，没有伪造图片 | 满足 | 润色版增加图注建议但不生成图 |
| 完成后进行自我审计并润色 | 本文件为审计产物 | 进行中 | 根据审计生成 v2 润色版 |

## 3. 总基线符合性检查

### 3.1 模块独立性

第一版的三个模块都有独立的需求、方案、创新和可行性结论。导航模块虽提到 Web 路线输入，控制台模块虽包含车辆和 PCR 数据，扩散模块虽使用采样坐标，但这些属于必要接口说明，没有要求读者按固定技术链阅读，符合总基线。

### 3.2 任务覆盖

八个任务 ID 均出现在第一版：

- H-01：RTK 硬件整合与定位测试；
- H-02：模拟农田实物场景和标准地图；
- A-01：RTK/SLAM 融合算法与 ROS 节点；
- A-02：扩散算法重构、效率和精度；
- D-01：Windows/Linux 安装包和 Android APK；
- D-02：新算法与高斯烟羽对比图表；
- D-03：牛耕式自主导航视频；
- D-04：安全门避障视频。

自动检查发现全部任务 ID 至少出现三次，正文中共有八处显式“待回填”标记。任务覆盖完整。

### 3.3 时间要求

第一版已于总基线规定的 2026-09-09 之前形成，不占用 9 月 10 日后的整合打磨窗口。

## 4. 事实与技术准确性审计

### 4.1 导航模块

已正确处理：

- 现有 ATGM336H 被明确写为普通单点 GNSS，而非 RTK；
- `<0.5 m` 被写为目标和 H-01 待回填指标，没有写成当前实测；
- TF 唯一发布者、`route_v1.json`、Pure Pursuit 参数、安全扇区和小尺度测试与当前项目一致；
- 仿真地图重复性与实车定位精度被明确区分；
- D-04 的 `±45°` 展示要求与当前默认 `±90°` 参数均被说明。

需要修改：

1. “轮速、IMU 与 SLAM 维持局部轨迹”的说法不够精确。应改为轮速/IMU EKF 提供连续 `odom`，SLAM 提供地图约束与 `map→odom` 修正；
2. RTK 融合设计应明确推荐以 `field/earth→map` 对齐或质量门控全局约束实现，禁止第二个节点重复发布 `map→odom`；
3. 增加分阶段落地路径：本地建图、坐标标定、RTK 接入、质量门、路线执行、结果评估；
4. 将 failsafe 失败作为已知风险和改进依据，不要让该问题压过方案整体可行性。

### 4.2 控制台模块

已正确处理：

- 当前响应式 Web 与 D-01 未来安装包/APK 被区分；
- rosbridge 静止数据接收没有扩写成完整远程控制验收；
- 手机端直接速度控制被限制，物理急停没有被浏览器替代；
- localStorage 与企业后端设计被分层描述。

需要修改：

1. “安全策略建立在分级权限和操作审计基础上”可能被理解为 RBAC 和审计已经实现。润色版应改为“当前安全机制 + 目标权限体系”；
2. D-01 的具体封装技术尚未返回，不应暗示已确定 Electron、Tauri 或 Capacitor；保持“同一 Web 核心 + 最终封装方案”表述；
3. 增加企业价值：降低多端重复开发、便于跨地块监管、支持弱网边缘运行和后续 SaaS/私有化部署；
4. 增加多端交付的实施步骤和最低验收方法。

### 4.3 扩散预测模块

已正确处理：

- 保留高斯烟羽的稳态优势，没有宣称新算法全面优于；
- 当前三个场景被明确标注为合成数据；
- 训练点和留出测试点分离；
- PCR 物理单位链未闭合时使用相对/合成浓度；
- 侵染概率与真实发病被区分。

需要修改：

1. 明确正文中的平流—扩散方程属于目标重构方案，最终离散形式以 A-02 交付为准；
2. 第一轮 v1 数值来自离线动态 Eulerian 代理，不能直接写成 TypeScript 运行时算法的最终精度；
3. 增加逐时计算步骤、质量控制和异常降级，使可实现性更直观；
4. 当前结果段应压缩数值陈述，把创新结论放在动态场景适应性和可复现实验设计上；
5. 增加企业使用方式：风险分区、复检优先级、模型本地化标定和人工确认。

## 5. 创新性与可行性审计

| 模块 | 创新性表达 | 可行性表达 | 需要加强 |
|---|---|---|---|
| 导航 | 分层融合、版本化任务包、质量感知安全门 | 现有 ROS 栈、仿真、实车和在途任务 | 分阶段部署、维护和替代方案 |
| 控制台 | 单一 Web 核心、多端封装、统一数据契约、来源可追溯 | 七页签、响应式布局、构建测试和 rosbridge | 企业部署模式、当前/目标权限分界 |
| 扩散算法 | PCR 源反演、动态命运过程、侵染窗口 | TypeScript 引擎、Python 管线、合成对比 | A-02 最终算法与 v1 代理结果分界 |

第一版已经避免把“做了很多功能”直接等同于创新，但部分创新段落仍偏总结。润色版需要把每项创新都写成“传统方法局限—本方案机制—带来的工程价值”三段关系。

## 6. 文风与结构审计

优点：

- 正文完整，三模块分别包含 8、7、9 个小节；
- 技术术语基本统一；
- 图表和数据均有明确占位；
- 没有大段提纲式空白，可以直接并入总报告继续修改。

问题：

- 一些句子较长，工程信息密度过高；
- “当前、现有、尚未”等状态词较多，容易使报告呈现为项目周报；
- 个别段落先讲风险再讲价值，不利于企业方案的说服力；
- 三个章节的结尾结构相似但不完全一致。

润色策略：

1. 每节首句先给设计结论，再解释方法；
2. 将阶段证据集中到“可行性基础”段落，主体方法使用方案语言；
3. 风险采用“风险—影响—应对”结构，避免只列未完成事项；
4. 三模块结尾统一为“创新点—可行性基础—实施路线—适用边界”；
5. 保留专业性，但减少不必要的内部文件名和开发过程叙述。

## 7. 润色版必须完成的修改

- [x] 保持三模块独立，不新增强制技术链；
- [x] 导航章节精确区分 EKF 连续位姿、SLAM 地图约束与 RTK 全局对齐；
- [x] 为导航增加六步实施路线和可替代 RTK 方案；
- [x] 将控制台“当前安全机制”和“企业权限设计”拆开；
- [x] 为多端控制台增加交付部署和企业价值；
- [x] 明确扩散方程是 A-02 目标方案，v1 数据是离线代理结果；
- [x] 压缩状态汇报语言，加强创新机制和工程收益；
- [x] 统一所有占位格式为 `【任务 ID 待回填：内容】` 或 `【图/表占位：内容】`；
- [x] 各模块增加实施路径和适用边界；
- [x] 完成最终结构、任务 ID、公式和过度主张检查。

## 8. 审计判定

第一版不是最终交付版本。它已经满足“有完整初稿可供审计”的要求，但上述修改完成前，不应作为打磨后的初稿提交。下一步应据此生成 v2 润色版，并再次进行机械检查和逐项要求核验。

## 9. 润色版复核结果

复核对象：`报告书三模块初稿_润色版_v2_2026-09-06.md`。

### 9.1 要求覆盖

- 三个模块均为独立一级章节，分别具有企业需求、总体设计、关键方法、实施路线、创新性和可行性；
- 没有把三部分强制写成单一技术链，仅在使用说明中保留必要输入输出接口；
- H-01、H-02、A-01、A-02、D-01、D-02、D-03、D-04 全部进入对应正文；
- 所有未返回的实测、代码、安装包、图片和视频均使用任务 ID 占位；
- 当前已有仿真、实机、软件和合成结果均保留适用条件，没有扩大为产品级结论；
- 报告重心已经从项目状态清单调整为创新机制、系统设计、企业价值和工程实施路径。

### 9.2 技术复核

- 导航部分明确轮速—IMU EKF 发布连续 `odom`、SLAM 发布 `map→odom`、RTK 负责全局对齐，避免重复 TF 发布；
- 当前普通 GNSS、目标 RTK 和 H-01/A-01 预期成果明确分层；
- 当前障碍扇区默认 `±90°` 与 D-04 展示的 `±45°/0.5 m` 条件均被如实说明；
- 控制台明确区分当前连接锁、未来 RBAC，以及响应式 Web 基础和 D-01 多端封装；
- 扩散方程明确标注为 A-02 目标重构方案，v1 数值明确标注为离线代理合成结果；
- 高斯烟羽在稳态场景中的优势被保留，没有制造“新算法全面占优”的结论。

### 9.3 机械检查

- 润色版共 303 行、约 36.8 KB；
- 三个主体模块分别包含 8、8、9 个二级小节；
- 八个任务 ID 均多次出现；
- 展示公式起止标记平衡；
- 图、表、参考文献和成员成果占位均可检索；
- 未检出“RTK 已达标”“APK 已完成”“真实田间预警已验证”等无证据完成性表述。

### 9.4 最终判定

润色版已经满足本轮目标，可作为三部分内容的打磨后初稿交付。后续仅需根据成员成果和总报告目录回填数据、图表、引用编号及章节编号，不需要重写主体逻辑。
