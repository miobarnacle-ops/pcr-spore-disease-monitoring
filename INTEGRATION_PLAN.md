# 孢子病害巡检机器人与 Web 控制台整合计划

## 1. 目标与结论

本计划面向竞赛要求，将以下两项阶段成果整合为一套“规划—执行—采样—诊断—预警—处置”闭环系统：

- `spore-patrol-robot-feature-lidar`：ROS 2 Humble 机器人端，负责底盘、雷达、里程计、IMU、本地 EKF、建图与硬件安全。
- `spore-monitor-web`：Next.js/React Web 控制台，负责地块规划、环境与孢子数据、PCR、病害影像、扩散预测、监管看板和机器人监控。

推荐总体边界：**机器人端负责实时控制和安全，网页端负责任务编排、数据汇聚、可视化和决策**。浏览器不得成为失联停车、避障或自动任务状态机的唯一执行者。

竞赛演示的最小闭环应为：

```text
地图/地块边界 → 生成巡检路线 → 路线校验与下发 → 机器人自主巡检
→ 到点停车与采样 → 数据绑定位置/时间/样本 → 风险识别与3/5/7天预测
→ 地图热力图与异常告警 → 人工确认/处置 → 全流程报告与回放
```

## 2. 现有能力审计

| 能力 | 当前状态 | 整合判断 |
|---|---|---|
| 底盘控制与安全 | 已有 `/cmd_vel`、`/odom`、电池、诊断、0.30 s 看门狗及失联停车 | 可作为执行底座；网页遥控仅用于低速调试 |
| 激光雷达 | X3 Pro 已发布 `/scan`，实物方向与 RViz 已联调 | 可立即接入网页点云；仍需室外强光和长稳测试 |
| 本地定位 | 轮速 + ICM20948 EKF，输出 `/odometry/filtered` | 网页当前读 `/odom`，应改为优先读取融合位姿 |
| SLAM 地图 | SLAM Toolbox 配置及 PGM/YAML 导入链路已有 | 可作为网页规划与轨迹叠加底图 |
| 北斗 + SLAM | RTK、航向、CORS/NTRIP、全局融合未完成 | 是定位误差 `<0.5 m` 的关键阻塞项 |
| 自动导航 | Nav2、动态避障、覆盖路径未完成 | 是路线“可下发、可执行”的关键阻塞项 |
| 采样任务 | “到点—停车—采样—确认—继续”状态机未完成 | 是业务闭环关键阻塞项 |
| 网页规划 | 边界、采样点、路线、`route_segments`、米制航点已存在 | 需升级为有版本、坐标系和任务属性的标准任务包 |
| 实时监控 | rosbridge 已接 `/odom`、`/scan`、`/cmd_vel`，支持轨迹导出/回放 | 已有雏形；缺任务、定位质量、电池、诊断和告警话题 |
| 环境/PCR/预测 | 环境、孢子、PCR、融合热力图、3/5/7 天预测与报告已有 | 当前多为手工/模拟数据，应接入真实数据和持久化服务 |
| 病害识别 | 数据库含 8 种病害，现为浏览器颜色/纹理原型分类 | 不能据此声称准确率 ≥90%，需真实模型与独立测试集 |
| 数据存储 | 主要为 `localStorage` 和浏览器导出 | 不适合多设备、监管与追溯，需服务端数据库和文件存储 |
| 高清视频 | 当前未发现视频接入 | 需新增相机、视频流和网页播放器 |
| 极端气候连续运行 | 软件有部分掉线保护，硬件环境适配未闭环 | 需防护、温度、供电、通信及断点续传专项验证 |

## 3. 目标架构

```text
Web 控制台（电脑/手机）
  ├─ 地块地图、路线编辑、任务创建、审批与下发
  ├─ 机器人位置/轨迹/雷达/视频/电池/定位质量
  ├─ 环境、孢子、影像、PCR、热力图、3/5/7天预警
  └─ 告警确认、处置记录、报告与历史回放
             │ HTTPS + WebSocket
任务与数据服务（建议 FastAPI 或 NestJS，比赛可部署在车载机/局域网服务器）
  ├─ REST API：地块、任务、样本、检测、预测、告警、报告
  ├─ 数据库：PostgreSQL；空间数据可启用 PostGIS
  ├─ 文件存储：地图、图片、视频片段、PCR 报告、模型版本
  └─ ROS 网关：鉴权、限流、消息转换、断线重连、数据落库
             │ ROS 2 / rosbridge（仅局域网或受控 VPN）
机器人 ROS 2
  ├─ localization：轮速 + IMU + RTK + SLAM，全局/局部定位
  ├─ navigation：Nav2 全局规划、局部避障、Collision Monitor
  ├─ mission：任务校验、到点采样、重试、跳过、返航、故障
  ├─ sensors：雷达、相机、环境、孢子、GNSS、电池、诊断
  └─ base：速度限制、硬件急停、命令看门狗和失联停车
```

部署优先级：比赛阶段先采用“车载工控机 + 路由器/4G + 浏览器”的边缘部署，保证外网中断时仍能巡检、缓存和查看；有稳定网络后再增加云端监管汇总。

## 4. 核心接口契约

### 4.1 统一坐标与版本

所有地图、路线、轨迹和采样结果必须带：

- `field_id`、`map_id`、`map_version`、`route_id`、`mission_id`；
- `frame_id=map`、单位 `m`、坐标类型 `local_enu`；
- 田块 ENU 原点经纬高、坐标基准（WGS84/CGCS2000）和 X 轴航向；
- 设备时间戳、服务端接收时间戳和数据质量标志。

网页当前输出的 `planning.json` 应升级为任务包 `schema_version: 1.0`，至少包含：

```json
{
  "meta": {
    "schema_version": "1.0",
    "field_id": "field-001",
    "map_id": "map-001",
    "map_version": 1,
    "route_id": "route-001",
    "frame_id": "map",
    "unit": "m"
  },
  "coordinate_system": {
    "type": "local_enu",
    "origin_latitude_deg": null,
    "origin_longitude_deg": null,
    "origin_altitude_m": null,
    "x_axis_heading_deg": null,
    "crs": "CGCS2000"
  },
  "mission": {
    "start_pose": { "x_m": 0, "y_m": 0, "yaw_rad": 0 },
    "return_pose": { "x_m": 0, "y_m": 0, "yaw_rad": 0 },
    "minimum_return_battery_pct": 30
  },
  "waypoints": [
    {
      "id": "WP-001",
      "x_m": 0,
      "y_m": 0,
      "yaw_rad": null,
      "type": "sampling",
      "sample_id": "S-001",
      "speed_limit_mps": 0.2,
      "tolerance_m": 0.2
    }
  ]
}
```

任务下发前必须校验地图版本、边界/膨胀区、单位、航点连续性、曲率、起返点、预计电量和采样点完整性。失败任务不得进入自动执行。

### 4.2 ROS 2 接口

| 方向 | 接口 | 用途 |
|---|---|---|
| 机器人→平台 | `/odometry/filtered` | 优先展示融合位姿、速度和协方差 |
| 机器人→平台 | `/scan` | 雷达点云与障碍态势 |
| 机器人→平台 | `/battery_state` | 电压、电量、低电量返航判断 |
| 机器人→平台 | `/diagnostics` | 传感器、串口、低电压和数据超时 |
| 机器人→平台 | `/fix`、`/gps/rtk_status` | 北斗位置与 Single/Float/Fix 质量 |
| 机器人→平台 | `/mission/status` | 状态、当前航点、进度、ETA、故障码 |
| 机器人→平台 | `/plan`、`/local_plan` | 计划路径、局部绕行路径 |
| 机器人→平台 | `/sampler/status` | 就绪、工作、完成、失败、超时 |
| 机器人→平台 | `/sensor/environment` | 温湿度、墒情、光照、雨量、叶湿 |
| 机器人→平台 | `/sensor/spore` | 浓度、单位、质量、采样位置和样本号 |
| 机器人→平台 | `/system/alarm` | 定位、障碍、电池、采样和设备告警 |
| 平台→机器人 | `/mission/command` 或 ROS Action | 加载、启动、暂停、继续、取消、返航 |
| 平台→机器人 | `/sampler/start` | 调试或任务状态机触发采样 |
| 平台→机器人 | `/cmd_vel` | 仅授权调试模式的低速点动，不作为自动巡检主接口 |

任务执行建议使用 ROS 2 Action/Service；连续遥测使用 Topic。网页不应通过持续发送 `/cmd_vel` 实现自动路线跟踪。

### 4.3 数据 API 与最小数据模型

首期 REST 资源：`fields`、`maps`、`routes`、`missions`、`robots`、`samples`、`environment_readings`、`spore_readings`、`images`、`pcr_reports`、`predictions`、`alerts`、`reports`。

每条诊断数据统一关联：

```text
field_id + mission_id + waypoint_id + sample_id
+ timestamp + map pose + GNSS + quality + source_device + algorithm_version
```

这组关联键是生成地块热力图、扩散趋势、审计报告和复现实验的基础。

## 5. Web 控制台整合内容

保留现有 7 个页签，并按闭环调整：

1. **路径规划**：同时显示 SLAM 占据栅格和田块 ENU/卫星底图；生成标准任务包；显示禁行区、转弯半径、路径有效性和预计电量。
2. **环境监测**：模拟/真实数据源显式切换；实时曲线、数据新鲜度、传感器离线标志；采样点自动写入环境与孢子读数。
3. **PCR 分析**：PCR 报告上传后按 `sample_id` 自动匹配，不再依赖人工选择坐标；保留质量、LOD 和离群检查。
4. **病害识别**：调用服务端模型推理，展示模型版本、Top-K、置信度和人工复核；保留混淆矩阵、精确率、召回率和 F1。
5. **趋势预警**：真实历史数据驱动 3/5/7 天预测；展示输入完整性、模型版本、预测区间和回测结果；支持高风险区生成复检任务。
6. **监管中心**：多地块地图、设备在线率、告警闭环、任务成功率、病害态势和报告归档。
7. **机器人监控**：融合位姿、RTK 状态、路径与进度、雷达、高清视频、电池、诊断、任务控制及轨迹回放。

机器人监控页的安全要求：

- 手动遥控必须先进入“人工接管模式”，有速度上限、按住运行/松手停车和超时归零；
- “网页急停”命名改为“软件停车”，并保留独立物理急停；
- 网络断开时机器人端自主停车或按策略返航，不能等待网页指令；
- 所有任务命令记录操作者、时间、旧状态、新状态和结果。

## 6. 分阶段实施计划

### 阶段 0：冻结基线与接口（2–3 天）

- 分别保存两项成果的可运行版本、运行命令、测试结果和演示数据。
- 确认实车仍为当前 WHEELTEC 高配摆式四驱底盘，消除文档中的 R680/C50X 命名差异。
- 冻结任务 JSON Schema、坐标系、ROS 话题、状态枚举和错误码。
- 输出一张端到端数据字典及接口模拟样例。

验收：同一任务包能被网页导出、机器人端离线校验器读取；错误地图版本和错误单位会被拒绝。

### 阶段 1：实时监控最小闭环（4–5 天）

- 机器人端增加 rosbridge 启动入口和受控网络配置。
- 网页改读 `/odometry/filtered`，补订阅电池、诊断和连接新鲜度。
- 使用现有 PGM/YAML 地图叠加计划路线、实际轨迹和当前位姿。
- 加入模拟/实车模式，避免模拟数据混入比赛统计。

验收：连续运行 30 分钟，页面位置、雷达、电池和诊断无明显卡死；断网后底盘在看门狗时限内停止；重连后状态恢复。

### 阶段 2：Nav2 与任务执行闭环（7–10 天）

- 新增 `spore_patrol_navigation`：静态地图定位、全局规划、局部避障、路径跟踪、Collision Monitor。
- 新增 `spore_patrol_mission`：任务加载/校验、导航、接近、稳定、采样、确认、下一点、返航、故障。
- 网页新增任务创建、下发、启动、暂停、继续、取消、返航和实时进度。
- 路线经过平滑与完整矩形足迹碰撞检查；垄间速度与转弯速度分级限制。

验收：在仿真和实车测试场完成不少于 5 点的“自动到点—停车—模拟采样—继续—返航”；记录到达率、停靠误差、任务耗时和失败恢复。

### 阶段 3：北斗 + SLAM 融合定位（7–14 天，与阶段 2 可交叉）

- 完成 RTK 选型、CORS/NTRIP 或自建基站链路、天线安装和时间同步。
- 发布 `/fix`、航向和 RTK 状态；建立经纬度到田块 ENU 的转换。
- 用 `robot_localization` 或等效方案融合 RTK、IMU、轮速和激光定位，明确 `map→odom→base_footprint` 唯一发布者。
- 网页展示 Fix/Float/Single、协方差、定位源降级和误差告警。

验收：用独立参考轨迹测量 RMSE、95% 分位和最大误差，满足竞赛定位误差 `<0.5 m`；垄间工程目标另按横向 RMSE `≤0.10 m` 验证，若达不到则扩大通道或提高安全策略。

### 阶段 4：真实采样与多模态数据链（7–10 天）

- 明确采样装置的触发、完成、失败、超时接口和样本编码规则。
- 环境、孢子、影像、PCR 全部使用统一 `sample_id`，自动绑定任务、位置和时间。
- 落地服务端数据库、文件存储、断网缓存与恢复上传。
- 高浓度或诊断阳性自动创建告警，并可生成高风险区复检路线草案。

验收：任选一个样本可从地图点追溯到原始传感器值、照片、PCR 报告、模型版本、告警和处置结果；网络中断数据不丢失。

### 阶段 5：病害识别与预警达标验证（10–20 天，取决于数据）

- 选择与比赛场景一致的 5–8 种病害，冻结类别定义、采集规范和标注标准。
- 使用独立训练/验证/测试划分，防止同株、同视频相邻帧泄漏到不同集合。
- 服务端部署真实分类/检测模型；网页只展示推理、复核和指标。
- 用多季节/多地块历史数据回测 3/5/7 天风险预测，定义“提前预警”的事件和命中窗口。

验收：独立测试集综合准确率 `≥90%`，同时报告各类样本数、混淆矩阵、宏平均 F1；3–7 天预警报告命中率、漏报率、误报率和提前量分布。数据不足时只能标注“原型验证中”，不得以模拟数据代替达标证据。

### 阶段 6：视频、全域监管与可靠性（5–10 天）

- 接入相机与低时延视频（优先 WebRTC；演示备选 HLS/MJPEG）。
- 完成多地块、多机器人、告警派单、处置闭环和移动端响应式页面。
- 进行高低温、降雨/粉尘、防晒、供电、4G 弱网、重启恢复和 8 小时长稳测试。
- 建立一键演示脚本、故障降级预案和离线演示数据包。

验收：设备离线、RTK 失锁、雷达超时、低电量、采样失败、障碍阻塞均能产生明确状态与告警；恢复后任务与数据可追溯。

## 7. 竞赛指标验收矩阵

| 竞赛要求 | 证据与验收方法 | 当前判断 |
|---|---|---|
| 5–8 种病害、准确率 ≥90% | 冻结独立测试集、类别样本数、混淆矩阵、宏 F1、模型版本 | 有 8 类数据库，但原型分类器未达标证明 |
| 北斗 + SLAM，误差 <0.5 m | 独立真值轨迹；RMSE、P95、最大误差；RTK 状态日志 | RTK 与全局融合未完成 |
| 提前 3–7 天预警 | 历史事件回测；命中/漏报/误报、提前量、阈值版本 | 有算法与界面，缺真实回测证据 |
| 多维数据与热力图 | 同一位置/样本的环境、孢子、影像、PCR 关联；可视报告 | 页面能力较全，真实采集与持久化待接 |
| 高清视频与远程查看 | 分辨率、帧率、端到端延迟、弱网恢复 | 未接入 |
| 地图规划—自动执行—告警联动 | 任务日志、轨迹、状态机、异常处置闭环 | 规划和监控已有，自动执行未完成 |
| 极端气候连续监测 | IP 防护、温度范围、供电、断网缓存、长稳报告 | 尚无完整证据 |
| 远程可控与无人作业 | 授权、审计、人工接管、硬件急停、失联安全 | 有低速遥控雏形，任务与安全闭环待补 |

## 8. 优先级与人员建议

必须优先完成（P0）：坐标/任务协议、Nav2、任务状态机、融合定位、采样接口、安全与真实数据落库。这些决定演示是否形成真实闭环。

随后完成（P1）：服务端病害模型、预警回测、高清视频、多地块监管和报告证据链。

最后增强（P2）：复检点自动生成、云端部署、更多作物/病害、智能施药联动和高级统计。

建议至少明确四个责任面：机器人导航与定位、Web/后端与数据、采样/传感器与硬件、病害模型/PCR 与试验。每个接口指定唯一负责人、样例数据、完成日期和验收记录。

## 9. 主要风险与止损策略

- **坐标不一致**：没有 ENU 原点、轴向和地图版本时禁止下发任务；先在小场地完成三点标定。
- **浏览器直控风险**：自动任务只走 ROS Action/任务状态机；保留底盘看门狗、Collision Monitor 和物理急停。
- **指标被模拟数据污染**：所有记录标记 `real/simulated`；比赛指标仅统计真实、冻结测试集。
- **RTK 在树冠/遮挡区失锁**：显示定位质量，降速后短时以 IMU/轮速/激光维持，超时停车告警。
- **X3 Pro 室外性能不足**：尽快做强光、植株、粉尘长稳测试；不满足时预留更高防护等级雷达替换接口。
- **数据量不足以证明预警**：将“算法演示”和“真实回测达标”分开汇报，优先积累连续时序数据。
- **单机 localStorage 丢失**：尽早切换到服务端数据库；边缘端离线队列按唯一事件 ID 幂等补传。
- **时间不足**：先打通 5 点闭环和一类真实采样链，再扩展多地块、视频和智能复检。

## 10. 第一周可执行清单

1. 统一底盘型号、ROS 话题、TF、坐标原点和任务状态枚举。
2. 提交任务 JSON Schema、正确/错误样例及机器人端校验脚本。
3. 为机器人增加统一 rosbridge 启动入口；网页切换到融合位姿并补电池/诊断订阅。
4. 在 Gazebo 中把网页路线转换为 5 个模拟导航目标，先用模拟采样状态跑通。
5. 确定 RTK/CORS、采样器、相机和环境传感器的实际硬件及交付日期。
6. 建立真实数据表和统一 `sample_id`，停止把新增正式数据只存于 localStorage。
7. 建立竞赛验收台账：每一项指标对应原始日志、视频、数据集版本和统计脚本。

