import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "cn.suixun.showcase",
  appName: "穗巡",
  webDir: "dist",
  android: {
    backgroundColor: "#0c151a",
  },
};

export default config;
