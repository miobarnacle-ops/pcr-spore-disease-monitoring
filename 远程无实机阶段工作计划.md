# 远程无实机阶段工作计划

> 制定日期：2026-08-30  
> 适用场景：仅访问当前项目文件与本地开发环境，不连接树莓派、底盘、雷达、GNSS、采样器或相机，不发送任何实车运动指令。

> 后续状态更新（2026-08-30）：W0–W9 远程阶段成果完成后，已在用户明确要求下进行一次受控实机恢复。恢复结果、服务配置和静止验收证据以总基线为准；本文件中“远程无实机”描述保留为阶段历史边界，不覆盖后续实机记录。

## 1. 总基线与工作边界

### 1.1 项目总基线

- 项目下的 `机器人车辆导航子系统工作计划与agent协作指南.md` 是全部工作的总基线和最高层级执行依据。
- 本计划是总基线在“远程且不连接实机”条件下的阶段性拆解，不替代总基线，也不单独改变其中已经冻结的项目决策。
- 当本计划、Web 文档、车辆端文档、历史对话或代码注释之间出现冲突时，先以总基线为准；如总基线与最新实测事实不一致，应先记录证据并更新总基线，再继续实施。
- 工作状态、决策、风险、完成证据和遗留问题最终都应回填总基线，避免形成独立且失真的平行计划。

### 1.2 各子系统执行依据

- 控制台整合以本地 `spore-monitor-web/` 为开发基线，但其范围、优先级和接口边界服从项目总基线。
- 功能基准以 `揭榜挂帅.zip/软著/main_inspection_system_v4_compliance.py` 为主，并参考同目录下的路径规划、田间检测和扩散预测 Python 模块。
- `spore-monitor-web/FUNCTION_PARITY.md` 作为已有迁移清单，但必须重新用源码和自动化测试核验，不能仅凭文档判定“已完成”。
- 之前发布的线上“穗巡”空壳平台及其页面结构不作为产品、架构或验收基线；本阶段不进行线上发布。
- 车辆端实施以 `spore-vehicle-nav/` 的真实代码、配置和测试证据为执行依据，公开团队仓库和历史快照仅用于追溯。

基线层级如下：

```text
机器人车辆导航子系统工作计划与agent协作指南.md（项目总基线）
  ├─ 远程无实机阶段工作计划.md（本阶段执行拆解）
  ├─ spore-monitor-web/（本地控制台实现）
  │    └─ 揭榜挂帅.zip/软著/*.py（软著功能参考源）
  └─ spore-vehicle-nav/（车辆端实现与验证证据）
```

### 1.3 本阶段允许完成的工作

- Python 软著功能与本地 Web 控制台的逐项对照、补齐和回归测试。
- 路线任务、机器人遥测、任务状态、采样器、GNSS/RTK 和告警的接口契约设计。
- 纯逻辑算法、状态机、数据转换和异常处理的单元测试。
- 使用 Mock/Replay 数据完成控制台整合，不伪装成实机数据。
- 本地构建、类型检查、代码规范、服务端渲染和离线演示验证。
- 实机接入脚本、检查清单、数据记录模板和现场测试用例的预先准备。

### 1.4 本阶段不能宣称完成的工作

- 不能宣称底盘、雷达、IMU、GNSS、RTK、相机或采样器已接入成功。
- 不能验证实车跟踪误差、定位精度、RTK Fix、避障距离、停车时延和失联停车。
- 不能以模拟数据证明病害识别准确率、3–7 天真实预警效果或田间检测效果。
- 不能把自身里程计或模拟轨迹当作独立真值。
- 不能向 `/cmd_vel` 或任何实机控制接口发送指令。

## 2. 阶段目标

在不连接实机的条件下，形成一套可本地运行、可重复测试、接口清晰的控制台整合基线，使后续连接实机时主要工作变成“替换数据源和现场校准”，而不是临时修改业务逻辑。

远程阶段的最小演示闭环为：

```text
导入地块/地图
  → 生成并校验巡检路线
  → Mock 机器人加载任务
  → 模拟行驶、到点、停车、采样、继续和返航
  → 环境/孢子/PCR 数据绑定 sample_id
  → 生成融合热力图和 3/5/7 天预测
  → 注入断联、定位降级、低电量或采样失败
  → 控制台显示告警、恢复策略、事件日志和报告
```

所有 Mock/Replay 数据必须在界面和导出结果中明确标注 `simulated` 或 `replay`。

## 3. 工作包总览

| 编号 | 优先级 | 工作包 | 预计投入 | 主要产出 | 无实机可验收 |
|---|---|---|---:|---|---|
| W0 | P0 | 冻结本地基线与软著参考源 | 0.5 天 | 基线说明、源文件哈希、运行命令 | 是 |
| W1 | P0 | Python→Web 功能对照审计 | 1–2 天 | 逐项差距矩阵、缺口优先级 | 是 |
| W2 | P0 | 核心算法兼容性回归与增强边界 | 1–2 天 | Python/TypeScript 关键样例、兼容性测试与增强说明 | 是 |
| W3 | P0 | 路线与任务接口契约 | 1 天 | JSON Schema、状态枚举、错误码、样例 | 是 |
| W4 | P0 | Mock/Replay 数据总线 | 2 天 | 确定性机器人与传感器模拟器 | 是 |
| W5 | P0 | 机器人监控页整合 | 2 天 | 位姿、任务、定位、诊断、采样和告警界面 | 是 |
| W6 | P1 | 本地任务与数据持久化 | 1–2 天 | 版本化本地数据仓库、导入导出 | 是 |
| W7 | P1 | 车辆端离线鲁棒性测试 | 1 天 | 异常路线、状态机和故障场景测试 | 是 |
| W8 | P1 | 一键质量门与离线演示 | 1 天 | 自动检查脚本、演示数据包、结果摘要 | 是 |
| W9 | P1 | 实机接入前置包 | 0.5–1 天 | 话题清单、参数表、现场检查与记录模板 | 是 |

建议顺序：`W0 → W1/W2 → W3 → W4 → W5 → W6/W7 → W8 → W9`。其中 W2 的目标是守住明确继承的业务语义和冻结接口，不要求 Web 与软著逐项或逐数值完全一致；经确认的 Web 增强能力应单列基线和测试。

## 4. 详细任务清单

### W0：冻结本地基线与软著参考源【已完成 2026-08-30】

- [x] 读取总基线中的当前阶段、冻结决策、风险和待办，建立本计划任务与总基线章节的对应关系。
- [x] 记录 `spore-monitor-web`、`spore-vehicle-nav` 当前版本和工作树状态。
- [x] 从 `揭榜挂帅.zip` 中只提取软著功能核验所需的 Python 文件，排除压缩包内的 `.git`、依赖缓存和构建产物。
- [x] 对 `main_inspection_system_v4_compliance.py` 及其直接依赖计算 SHA-256，防止后续对照基准漂移。
- [x] 记录本地控制台标准运行方式：`npm run dev`，默认只绑定本机开发地址。
- [x] 明确总基线、线上旧平台、本地控制台、软著参考源和车辆端代码的层级与边界，写入项目说明。

验收标准：任何成员能准确指出项目总基线，并能说明“阶段计划、软著参考源、本地控制台、车辆端代码”各自所在位置、层级及用途。

### W1：Python→Web 功能对照审计【已完成 2026-08-30】

- [x] 从 Python 主程序中提取菜单、页签、输入参数、默认值、计算公式、状态字段和导出格式。
- [x] 对照 `FUNCTION_PARITY.md` 和 Web 实际代码，逐项标记：`完成`、`部分完成`、`仅模拟`、`缺失`、`行为不一致`。
- [x] 重点核验以下模块：
  - [x] 地块边界、比例尺、垄向、采样点和多轮路线规划；
  - [x] 风险阈值、四种模拟分布、单点/全检流程；
  - [x] Ct→浓度、质量判定、LOD、离群值和粗精融合；
  - [x] 气象/表型参数、3/5/7 天预测和报告导出；
  - [x] 历史项目、工作会话、CSV/JSON/TXT/GeoJSON/HTML 导入导出。
- [x] 对每个差异记录 Python 位置、Web 位置、预期行为、当前行为和修复建议。
- [x] 将“软著原功能”和“后续新增功能”分栏，避免扩散引擎、机器人监控等新增能力被误写成软著原有能力。

计划产出：`spore-monitor-web/docs/PYTHON_WEB_PARITY_AUDIT.md`。

完成记录：`远程无实机基线清单.md`、`spore-monitor-web/docs/PYTHON_WEB_PARITY_AUDIT.md`。

验收标准：功能矩阵中的每一项都能追溯到源码或测试，不使用“看起来已实现”作为完成依据。

### W2：核心算法兼容性回归与增强边界【已完成 2026-08-30】

- [x] 从 Python 参考程序选取固定输入，生成不依赖当前时间和随机数的黄金样例。
- [x] 为下列明确继承的逻辑建立 Python 与 TypeScript 兼容性测试：
  - [x] 像素坐标、米制坐标、旋转和垄向转换；
  - [x] 候选采样点、覆盖率、路线距离和预计时间；
  - [x] 风险等级和阈值边界；
  - [x] Ct→浓度、PCR 判定、Pearson 相关和离群值；
  - [x] 预测输入参数、输出网格和报告统计字段。
- [x] 随机算法仅对已声明的确定性/固定种子增强测试回归；浮点结果定义允许误差，不要求字符串完全一致，也不将 Python 的历史实现视为唯一正确实现。
- [x] 对有意升级的算法建立“继承行为/增强行为”边界说明；只有确有双模式价值时才保留兼容模式，不强制复制旧流程。
- [x] 增加空数据、极小地块、自交边界、重复点、非法数值和极端参数测试。

计划产出：`spore-monitor-web/tests/parity/`、`spore-monitor-web/tests/fixtures/soft-copyright/`。

完成记录：`spore-monitor-web/tests/parity/soft-copyright.test.ts`、`spore-monitor-web/docs/WEB_ENHANCEMENT_BOUNDARIES.md`。

验收标准：明确继承的核心公式、导出字段和冻结接口有自动化兼容性测试；Web 增强算法的输入、输出、差异原因和适用边界可追溯，不静默伪装为软著原功能。

### W3：路线、任务与遥测接口契约【已完成 2026-08-30】

- [x] 冻结 `route_v1.json` 的版本、坐标系、单位、必需字段和兼容规则。
- [x] 定义任务状态：`idle`、`loaded`、`running`、`approaching`、`sampling`、`paused`、`returning`、`finished`、`failed`、`cancelled`。
- [x] 定义任务事件：加载、启动、到点、采样开始、采样完成、跳过、暂停、恢复、返航、故障和人工取消。
- [x] 定义机器人遥测契约：
  - [x] `/odometry/filtered` 位姿、速度、协方差和数据时间；
  - [x] `/scan` 雷达范围和新鲜度；
  - [x] `/battery_state` 电压、电量和低电量状态；
  - [x] `/diagnostics` 设备健康和超时；
  - [x] `/fix` 与 RTK 的 `single/float/fix` 状态；
  - [x] `/mission/status` 任务、航点、进度、ETA 和故障码；
  - [x] `/sampler/status` 就绪、工作、完成、失败和超时。
- [x] 统一 `field_id + mission_id + waypoint_id + sample_id + timestamp + pose + quality` 关联键。
- [x] 为正常、缺字段、版本不兼容、过期数据和未知状态提供样例。

计划产出：`spore-monitor-web/docs/INTEGRATION_CONTRACTS.md`、TypeScript 类型、JSON fixtures 和车辆端兼容测试。

完成记录：`spore-monitor-web/lib/integration-contract.ts`、`spore-monitor-web/tests/contracts/`、`spore-vehicle-nav/src/spore_patrol_route_validation/spore_patrol_route_validation/mission_contract.py`。

验收标准：控制台与车辆端可分别使用同一组 fixtures 通过或拒绝数据，不依赖实机或 ROS 网络。

### W4：Mock/Replay 数据总线【已完成 2026-08-30】

- [x] 将控制台数据源明确拆分为 `mock`、`replay`、`rosbridge` 三种模式。
- [x] 默认开发模式使用 Mock，且界面持续显示“模拟数据”。
- [x] 实现确定性任务模拟器：加载路线→行驶→到点→采样→继续→返航→完成。
- [x] 模拟以下遥测：位姿、轨迹、雷达、电池、诊断、GNSS/RTK、任务进度和采样器状态。
- [x] 支持可控倍速、暂停、单步和重置，保证测试可重复。
- [x] 增加故障注入：
  - [x] rosbridge 断开与重连；
  - [x] 位姿超时或跳变；
  - [x] RTK `fix→float→single` 降级；
  - [x] 低电量；
  - [x] 雷达超时或静态障碍；
  - [x] 采样器忙、失败或超时；
  - [x] 任务被人工暂停或取消。
- [x] Replay 模式读取版本化 JSON 事件流，后续可直接替换为 rosbag 转换数据。
- [x] 无实机模式下禁用真实 `/cmd_vel` 发布，模拟运动仅更新本地状态。

验收标准：相同 seed 和事件脚本每次产生相同状态序列、轨迹和告警；故障场景不会被显示为“正常完成”。

完成记录：`spore-monitor-web/lib/robot-data-bus.ts`、`spore-monitor-web/docs/MOCK_REPLAY_GUIDE.md`、`spore-monitor-web/tests/contracts/robot-data-bus.test.ts`。

### W5：本地控制台整合【已完成 2026-08-30】

- [x] 保留软著迁移形成的业务页签和计算能力，不重建空壳界面。
- [x] 路径规划页增加任务包版本、地图版本、坐标系和校验结果摘要。
- [x] 机器人监控页统一展示：
  - [x] 数据源模式和连接状态；
  - [x] 融合位姿、轨迹、速度和数据新鲜度；
  - [x] 雷达、障碍状态、电池和诊断；
  - [x] GNSS/RTK 状态及“模拟/未接入”标志；
  - [x] 任务状态、当前航点、进度、ETA 和最近事件；
  - [x] 采样器状态、当前 `sample_id` 和失败原因；
  - [x] 可筛选的告警与事件时间线。
- [x] 将路线、任务、样本、PCR 和预测通过统一 ID 串联。
- [x] 增加任务加载、启动、暂停、继续、取消和返航按钮；Mock 模式只操作模拟器。
- [x] 明确“软件停车”与物理急停的区别，不把网页按钮描述成硬件急停。
- [x] 真实、模拟和回放数据采用不同视觉标识，并随导出结果保存来源。

验收标准：仅运行本地控制台即可完成第 2 节的演示闭环，并可从任一 `sample_id` 追溯任务、位置、环境、PCR、预测和告警。

完成记录：`spore-monitor-web/components/mission-runtime-panel.tsx`、`spore-monitor-web/lib/use-robot-data-bus.ts`；Web 完整质量门（lint、类型检查、数据总线/契约/兼容性/路线/引擎/渲染）通过。W5 完成时 Mock、Replay 与 rosbridge 均保持显式连接且未连接实机；后续实机基础 rosbridge 链路已在总基线 v0.8.3 中单独验证。

### W6：本地任务与数据持久化【已完成 2026-08-30】

- [x] 先抽象数据仓库接口，避免页面直接散落调用 `localStorage`。
- [x] 为地块、地图、路线、任务、样本、读数、PCR、预测、告警和事件定义版本化模型。
- [x] 第一阶段继续采用本地存储，不引入线上账号或云数据库依赖。
- [x] 增加完整会话导出/导入、schema 迁移和损坏文件拒绝。
- [x] 区分用户录入、模拟生成、回放导入和算法派生数据。
- [x] 对敏感信息、设备凭据和网络密码保持不落库、不导出。

验收标准：浏览器刷新或重启后可恢复任务；旧版本会话可迁移；损坏或未知版本不会覆盖现有数据。

完成记录：`spore-monitor-web/lib/local-session-repository.ts`；会话升级为 `inspection_console_session / schema_version: "6.0"`，兼容迁移旧 `version: 5` 文件，未知版本、损坏 payload 和敏感键会被拒绝且不覆盖当前工作区。`npm run test:storage`（2 项）、lint、类型检查和渲染测试通过；全程只用浏览器本地存储。

### W7：车辆端离线鲁棒性测试【已完成 2026-08-30】

- [x] 保留并扩展 `spore-vehicle-nav/verify.sh` 的纯逻辑测试。
- [x] 增加任务 schema 版本、坐标单位、航点连续性和非法速度的拒绝测试。
- [x] 增加任务状态机正常流程与暂停、取消、返航、采样失败流程测试。
- [x] 将障碍判定、数据新鲜度和停车决策中可抽离部分做成纯函数测试。
- [x] 使用现有历史路线和日志制作只读回放 fixture。
- [x] 检查 ROS 节点在缺少 `/scan`、里程计超时、未知任务状态时是否进入安全状态；仅做代码和模拟验证，不宣称实车停车通过。

验收标准：车辆端全部离线测试通过；Web 导出的任务包能被车辆解析器读取；异常输入有稳定错误码。

完成记录：新增 `spore_patrol_route_validation/safety_decision.py`，将里程计/雷达新鲜度、前向障碍与未知状态的零速度判定提取为纯函数；ROS 跟踪节点接入该 fail-safe 门。路线解析器拒绝非连续航点序号及无 `sample_id` 的采样点；状态机具备暂停、继续、返航与取消的显式转移。`test_historical_route_fixtures.py` 以 `results/` 的既有路线作为只读回归输入；`./verify.sh` 通过 91 项 pytest 与 2 份 YAML 校验。本阶段没有做任何实车停车宣称。

### W8：一键质量门与离线演示【已完成 2026-08-30】

- [x] 建立统一检查顺序：格式、类型、单元测试、跨语言回归、构建和渲染测试。
- [x] Web 侧至少执行：

```bash
cd spore-monitor-web
npm run lint
npx tsc --noEmit
npm run test:engine
npm run test:route
npm test
```

- [x] 车辆侧至少执行：

```bash
cd spore-vehicle-nav
./verify.sh
```

- [x] 建立离线演示数据包：正常任务、RTK 降级、低电量、障碍停车、采样失败和断联恢复。
- [x] 自动生成测试摘要，分开列出“代码验证”“模拟验证”“待实机验证”。
- [x] 所有测试不依赖当前时间、外网、真实串口或开发者本机路径。

验收标准：新环境安装依赖后可重复运行；任一关键测试失败即阻止标记远程阶段完成。

完成记录：根目录 `verify_remote.sh` 已串行执行 Web lint/类型/兼容性、契约/数据总线/存储/演示、路线/引擎、构建/渲染测试及车辆 `verify.sh`；成功后自动生成 `remote_quality_gate_summary.md`。演示清单位于 `spore-monitor-web/tests/fixtures/demo/offline-demo-scenarios.json`，覆盖 6 个必需场景，`npm run test:demo` 验证其完整性。实际运行：Web 39 项专项测试加构建/渲染通过，车辆 91 项 pytest 与 YAML 校验通过；不访问外网、不连接 ROS/串口/实机。

### W9：实机接入前置包【已完成 2026-08-30】

- [x] 准备 rosbridge 地址、ROS_DOMAIN_ID、话题名、消息类型、频率和超时配置表。
- [x] 准备树莓派连接后只读检查顺序，不默认启动底盘运动。
- [x] 为 GNSS/RTK、采样器、相机分别准备最小接入样例和日志模板。
- [x] 准备现场实测表：定位 RMSE/P95、横向误差、到点误差、障碍距离、失联停车时间、任务成功率。
- [x] 准备实机测试红线：物理急停、人员站位、限速、轮子架空/落地条件、场地清空和失败止损。
- [x] 明确哪些远程模拟验收必须在实机上重新验证，不直接继承“通过”状态。

验收标准：获得实机连接后，无需临时设计接口即可按清单从只读检查逐级进入低风险测试。

完成记录：`spore-vehicle-nav/docs/实机接入前置包.md` 已提供参数/话题/频率/超时表、先只读后低速的检查顺序、RTK/采样器/相机契约样例、现场安全门与证据模板；`INTEGRATION_CONTRACTS.md` 同步车端 `PAUSED/RETURNING` 离线状态。地址保持 `ws://<车辆 IP>:9091` 占位，不保存设备凭据、不自动连接或启动底盘。

## 5. 两周建议排期

| 工作日 | 重点 | 当日退出条件 |
|---:|---|---|
| 1 | W0 基线冻结 + W1 审计框架 | 基线、参考源和差距矩阵模板完成 |
| 2–3 | W1 功能审计 + W2 黄金样例 | 关键软著功能均有源码对应关系 |
| 4 | W3 接口契约 | 正常/异常 fixtures 可被两端读取 |
| 5–6 | W4 Mock 任务与遥测 | 正常任务状态序列可确定性回放 |
| 7 | W4 故障注入 | 至少 6 类故障能稳定触发告警 |
| 8–9 | W5 控制台整合 | 本地完成规划→任务→采样→预测闭环 |
| 10 | W6 数据持久化 | 会话可恢复、迁移、拒绝损坏数据 |
| 11 | W7 车辆离线鲁棒性 | 车辆验证脚本及新增场景全部通过 |
| 12 | W8/W9 收口 | 一键检查、演示包和实机前置包完成 |

若时间不足，必须优先完成 W0–W5；W6 可先保留适配层，W9 可先输出检查表。

## 6. 里程碑与完成定义

### M1：功能基线可信

- Python 软著功能与 Web 控制台差距矩阵完成。
- 核心公式和导出字段有黄金样例。
- 新增功能与软著原功能明确分开。

### M2：接口可独立联调

- 路线、任务、遥测、GNSS、采样和告警契约冻结。
- Web 与车辆端使用同一套 fixtures。
- 错误版本、缺字段和过期数据可稳定拒绝。

### M3：本地模拟闭环完成

- 本地控制台完成正常任务全流程。
- 故障注入能触发正确状态、告警和恢复。
- 所有模拟数据均有清晰来源标识。

### M4：实机接入就绪

- Web 与车辆端自动检查全部通过。
- 离线演示数据包和测试报告可复现。
- 现场只读检查、低速测试和证据采集模板齐备。

## 7. 远程阶段风险控制

| 风险 | 控制措施 |
|---|---|
| 阶段工作脱离项目总基线 | 每个工作包关联总基线章节，完成状态和证据回填总基线 |
| 把旧线上空壳误当基线 | 只以本地 `spore-monitor-web` 和 Python 参考源做功能判断 |
| 模拟数据混入真实结论 | 数据模型、界面和导出统一保存 `source_mode` |
| 文档状态滞后于代码 | 完成状态必须绑定测试或源码位置 |
| Web 与车辆端接口再次漂移 | 共用 fixtures，双方测试同时校验 schema 版本 |
| 为远程演示过度重构 | 优先抽象数据源和契约，保留已迁移业务能力 |
| 无实机却误判安全能力 | 安全、精度和硬件性能始终标记“待实机验证” |
| 远程开发触发真实运动 | 无实机模式禁用真实控制发布，默认 Mock 数据源 |

## 8. 建议立即开始的第一批任务

1. 创建 Python→Web 功能差距矩阵，并从软著源码生成首批黄金样例。
2. 冻结任务、遥测、GNSS、采样器和告警接口契约。
3. 将控制台数据源重构为 `mock/replay/rosbridge` 三种明确模式。
4. 实现确定性任务模拟器和故障注入，再整合机器人监控页。
5. 把 Web 与车辆端测试合并为一套远程质量门。

第一轮完成后再决定是否进入本地持久化、报告整合和更多 UI 优化，避免在接口未稳定前扩大改动范围。
