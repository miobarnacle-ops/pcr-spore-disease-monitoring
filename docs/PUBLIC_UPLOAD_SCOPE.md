# 公开上传范围与脱敏记录

生成日期：2026-09-06；本次复核更新：2026-09-13

## 保留

- 项目源码、ROS 2 包、Web 控制台、离线算法和测试；
- 配置、接口契约、仿真世界、地图/轨迹/日志/回归证据；
- 计划、工作日志、报告素材、硬件资料和 PPTD/PPTX；
- 多端展示应用源码、Android 工程、验收证据和最终发行包；
- 报告书三模块的写作计划、初稿、润色稿、自审计和算法对比实验材料；
- 最新技术方案正文、企业提交版、北斗/真实建图补充版、成员章节和证据审计材料；
- 报告图表、公式图、成员材料摘录及可复现的报告制图脚本；
- 单垄仿真与真实建图的精选地图、对比图、参数和报告使用说明；
- `spore_patrol_teleop` 键盘遥控包、单垄仿真场景及车辆验证脚本更新；
- 车载 IMU 调用、零偏校准和 EKF 使用指南；
- 大型硬件视频、供应商压缩包、可执行文件和固件归档（Git LFS）；
- 历史总归档中可公开的源码、报告和资料（解包至 `legacy-materials/`）。

## 不上传

| 内容 | 处理 |
|---|---|
| 真实账号、密码、热点信息、SSH 私钥、令牌、实际 rosbridge 地址 | 删除或替换为占位符 |
| `spore-vehicle-nav/HANDOVER.md` | 不上传，由负责人单独提供私密版本 |
| `node_modules`、`.next`、ROS `build/install/log`、Python `.venv`、缓存 | 不上传，可重新生成 |
| 嵌套 `.git`、PID 文件、运行时元数据 | 不上传，统一仓库只保留工作树 |
| 本机绝对路径、内部 IP、个人联系方式、明文 sudo 口令 | 公开副本已脱敏/移除 |
| 根目录 `揭榜挂帅.zip` | 不原样上传；其可公开内容已提取，嵌套 Git、依赖缓存、申请/签章材料不公开 |
| 根目录会话导出 `08262204.json`、`08301247.json` | 不上传；其中包含历史凭据、网络配置和操作上下文，不属于项目运行所需源码 |
| `.openai/hosting.json` 等托管元数据 | 不上传；本地成员开发和离线演示不依赖托管项目标识 |
| 多端展示应用的 `dist/`、Gradle 缓存、Capacitor 生成资产、`win-unpacked/` | 不上传，可由源码和构建命令重新生成；仅保留最终 APK/安装包 |
| `.wrangler/`、`*.tsbuildinfo`、Python `__pycache__` 等构建缓存 | 不上传，可重新生成 |
| ROS 原始 rosbag、在线 SLAM 日志、位姿图和大批量中间输出 | 本次不上传；保留精选地图、指标图、参数和报告说明，原始数据由负责人按需通过安全渠道提供 |
| `report_assets/` 的 DOCX/PDF 渲染 QA 临时目录 | 不上传；保留报告图表、公式图、成员摘录和可复现脚本 |
| 实车部署文档中的真实主机名、内部 IP、本机绝对路径和个人联系方式 | 公开副本替换为 `<VEHICLE_HOST>`、`<VEHICLE_WORKSPACE>`、`<REPO_ROOT>` 或移除 |

## LFS

以下类型使用 Git LFS：`*.zip`、`*.mp4`、`*.exe`、`*.hex`。

```bash
git lfs install
git lfs pull
```

## 说明

这是从本机混合工作区整理出的公开统一仓库，不是多个 Git 历史的原样嵌套。历史文档中的“已完成”仍需结合日期和验证层级阅读；公开仓库不代表已具备实车环境。
