# 2026-09-13 项目进展增量同步记录

> 本次同步以公开仓库 `main` 分支提交 `a82dd45`（2026-09-07）为基线，补充 9 月 7 日之后形成的导航实测、Web 集成、报告材料和可复用证据。

## 1. 车辆导航与实车建图

- 新增 `spore_patrol_teleop` 键盘遥控 ROS 2 包：只发布标准 `/cmd_vel`，默认低速，退出或输入停止后发送零速度。
- 新增单垄 4.8 m 仿真场景、地图验证参数和单垄建图/制图脚本；正式仿真结果包含地图、参数、元数据和报告图片。
- 归档 2026-09-12 单垄真实绕行建图的精选证据：过滤后地图、里程计投图、参数文件、对比图和报告使用说明。
- 报告措辞保留证据边界：过滤后的真实数据支持“具备单垄建图可行性”，不扩大为“实时在线建图稳定通过”或“已满足自主导航精度要求”。

原始 rosbag、在线日志、位姿图和大批量中间输出未同步；需要复核原始数据时由负责人单独提供。

## 2. Web 控制台与算法集成

同步 `spore-monitor-web/` 的运行链路增强：

- 新增任务状态、机器人遥测、ROS Bridge、统一数据总线和本地会话存储的接口契约；
- 新增任务运行面板、离线演示场景、Mock/Replay 使用说明和 Python/Web 一致性审计；
- 保留扩散、感染风险、数据同化、模型对比及报告模拟实验的代码、测试和结果；
- 继续区分合成/离线对比结果与真实农田准确率，未将模拟结果表述为实测精度。

## 3. 报告与成果材料

新增或更新：

- 三项技术方案的技术深化版、北斗实测补充版和真实建图补充版；
- 企业提交版报告、图表增强版、导航成员成果整合稿、证据审计和终稿审计；
- 北斗 RTK 报告材料索取清单、R-02/R-03/北斗成员章节材料；
- `report_assets/` 中的报告图表、公式图、成员材料摘录和制图脚本；
- `spore-patrol-robot-feature-lidar/docs/IMU调用与使用指南.md`，说明 IMU 话题、校准和 EKF 调用方法；
- 多端展示应用的导航文档更新。

## 4. 公开边界

- 公开副本中的主机名、内部地址、本机路径和联系方式已替换为占位符或移除；
- 未上传账号、密码、热点信息、SSH 私钥、令牌、会话导出、托管元数据和私密交接文档；
- 未上传 ROS `build/install/log`、Python/Node/Gradle 缓存、Web 构建目录和多端解包目录；
- Windows 安装包等大文件继续按仓库既有 Git LFS 规则处理。

详细范围见 [`PUBLIC_UPLOAD_SCOPE.md`](./PUBLIC_UPLOAD_SCOPE.md)。

## 5. 本次同步前的检查

- `spore-vehicle-nav/verify.sh`：路线、GNSS、键盘遥控共 101 项测试通过，Python 语法和 YAML 校验通过；
- `spore-monitor-web`：lint、TypeScript 检查、算法/契约/存储/演示/数据总线/路线/一致性共 45 项测试通过；渲染 HTML 检查另有 2 项通过；
- 多端展示应用与桌面端脚本的结构检查通过；
- 以上均为本机离线检查，不等同于本次在树莓派上的重新部署或现场运动验收。
