# 自主巡检导航方案证据完整性审计

日期：2026-09-12

## 结论

现有成果足以支撑竞赛报告中的“完整方案、创新设计和工程可行性”论证，但不足以宣称所有性能指标已经完成最终验收。正式代表性实景 SLAM 建图是当前唯一尚未完成的主体实验；此外仍有若干已完成成果的原始证据需要归档，以及两项安全/精度结果需要继续验证。

## 已核验成果

| 对象 | 核验结果 | 可用于报告的结论 |
|---|---|---|
| 路线与 GNSS 软件测试 | 重新运行 `verify.sh`：91 项路线测试、5 项 GNSS 测试全部通过，Python 语法和车辆 YAML 检查通过 | 路线 schema、坐标处理、平滑、跟踪与 GNSS 解析代码具有自动测试基础 |
| 仿真 SLAM 地图 | 157×117 栅格、7.85 m×5.85 m、0.05 m/cell；占据/自由/未知为 1653/15871/845 | 地图保存与统计可复算 |
| SLAM 重复性 | 两次垄中心偏移均为 0.050 m；覆盖率分别为 100/100/100 与 100/100/99 | 仿真重复性低于项目 0.150 m 门槛 |
| 实车局部链路 | 临时单垄地图保存、U 形掉头、六类共 9 次小尺度路线记录 | 建图到车辆低速执行链路已具备实车基础 |
| RTK 阶段数据 | Fixed 区间内，对齐后 MAE 0.044 m、RMSE 0.049 m、P95 0.082 m、最大 0.117 m | 只能证明局部融合轨迹与 RTK ENU 的区间一致性，不能等同绝对精度 |
| 采样点规划 | test-3：8220.31 m²、两轮各 8 点、累计半高覆盖 88.37%、全半径几何覆盖 99.56% | 可作为离线规划阶段结果，不作为全尺度实车运行结果 |

## 必须补齐或归档的证据

1. 代表性真实垄作场景的正式 SLAM 地图：场景照片和尺寸、PGM/YAML、pose graph/data、同步 rosbag、重复建图及路线复跑结果。
2. K988 RTK 原始证据：rosbag、逐点误差 CSV、NTRIP/接收机配置、Fix 比例、卫星数、HDOP/协方差、差分龄期和分析脚本。
3. 独立真值定位评价：使用测量控制点、全站仪或独立高精度系统评价 RMSE、P95 和最大绝对误差；不能使用同一 RTK ENU 同时作为融合输入与绝对真值。
4. test-3 可复现包：原始田块边界、候选点、两轮选择结果、`route_v1.json` 与指标复算脚本。
5. 一次端到端任务证据：正式地图、RTK 状态、路线、采样驻留、障碍停车、轨迹和视频采用统一 `mission_id` 与时间基准。
6. 固件失联停车：现有约 1.309～1.409 s，高于项目 1.2 s 门槛；修正后需重复测试并留存日志。
7. 全程跟踪精度：补充独立真值下 P95 横向误差及多次完整路线结果。

## 报告措辞边界

- 可以写“方案形成闭环”“主要接口与分层链路已验证”“具有工程可行性”。
- RTK 数据应写为“固定解区间内轨迹一致性结果”，不得写成已经由独立真值证明的绝对定位精度。
- test-3 应写为“离线规划案例”，不得写成车辆已经完成 1081.14 m 真实田间运行。
- 当前 `local_ekf.yaml` 只融合轮速与 IMU；RTK 在线进入全局估计器尚需完成配置与节点集成。
- 仿真地图与临时单垄实车地图不能替代代表性真实农田正式地图。
