# 结构化模拟农田自主导航验证执行方案

## 1. 验证目的

本次不以制造完整农业机器人为目标，而是用现有小车证明以下技术链可行：

1. 路径规划算法能依据标准化垄参数生成可行路线；
2. 规划路线能转换为小车统一坐标系下的可执行路径；
3. 小车能在模拟结构化农田中完成自主跟踪、地头转弯和定点停车；
4. 过程数据能量化路线完成率、跟踪误差和重复性，为最终方案书提供证据。

验证最小闭环：

```text
输入田块与垄参数 → 网页生成路线 → 导出米制 ROS 路径
→ 小车加载路线 → 融合里程计定位 → 自动路径跟踪
→ 雷达安全停车 → 完成路线 → 导出轨迹和误差报告
```

本轮不把 RTK、真实病害识别、PCR、云平台和自动采样装置作为前置条件。采样点只需实现“到点停车 2 秒并记录事件”即可代表采样动作。

## 2. 推荐技术路线

### 首选：轻量路径跟踪验证

新增一个 ROS 2 路线适配/跟踪节点：

- 读取网页导出的标准米制路径 JSON；
- 将 `field` 路径转换到当前 `map` 或 `odom` 坐标系；
- 对折线路径做圆弧/样条平滑和等距重采样；
- 使用 Pure Pursuit 或 Regulated Pure Pursuit 跟踪；
- 订阅 `/odometry/filtered` 获取位姿；
- 发布 `/cmd_vel`，遵守底盘现有 0.15 m/s、0.30 rad/s 限制；
- 到采样点停车并发布事件；
- 雷达近距离障碍触发软件停车；
- 记录计划路线、实际轨迹、横向误差和状态。

该路线最适合阶段性背书：功能边界清楚、开发量可控，也能直接证明“自有规划算法已应用于实车”。

### 后续增强：Nav2 验证

在轻量闭环跑通后，再接入 Nav2 的地图定位、局部代价地图、Collision Monitor 和路径跟踪。Nav2 适合证明动态避障与工程可扩展性，但不应阻塞第一次路线跟踪试验。

## 3. 当前必须先解决的接口问题

现有网页导出的内容不能直接用于小车：

- `sampling_points[].x_m/y_m` 是米制；
- `route_segments[].waypoints` 仍是画布像素点；
- 浏览器画布 Y 轴向下，ROS 平面坐标 Y 轴通常向上；
- 路段包含直角折点，未满足四轮滑移底盘的转弯半径约束；
- 导出数据缺少路径坐标系、原点、起始姿态、地图版本和航点类型。

第一项代码任务应是升级网页路线导出器，输出统一格式：

```json
{
  "schema_version": "1.0",
  "frame_id": "field",
  "unit": "m",
  "origin_definition": "first_boundary_point",
  "x_axis_definition": "first_boundary_edge",
  "y_axis_definition": "left_of_x_axis",
  "vehicle": {
    "max_linear_mps": 0.12,
    "max_angular_radps": 0.25,
    "minimum_turn_radius_m": 0.5
  },
  "path": [
    { "seq": 0, "x_m": 0.0, "y_m": 0.0, "type": "start" },
    { "seq": 1, "x_m": 1.0, "y_m": 0.0, "type": "transit" },
    { "seq": 2, "x_m": 2.0, "y_m": 0.0, "type": "sampling", "sample_id": "R1-P1" }
  ]
}
```

建议在网页中固定规则：第一个边界点是 `field` 原点，第一个点到第二个点为 `+X`，田块内部位于 `+Y` 一侧。如果点击方向使田块落在右侧，导出器自动翻转 Y 轴或明确拒绝。

车辆开始试验时记录 `field` 原点在 ROS 坐标系下的位姿 `(x0, y0, theta0)`，转换公式为：

```text
x_ros = x0 + cos(theta0) * x_field - sin(theta0) * y_field
y_ros = y0 + sin(theta0) * x_field + cos(theta0) * y_field
```

所有坐标转换都应先在网页和 RViz 中叠加检查，通过后才允许发送运动命令。

## 4. 模拟农田建议

推荐首轮场地尺寸约 `8 m × 6 m`，根据实际空间等比例缩小：

- 2–3 条平行模拟垄；
- 通道净宽建议先用 `1.20 m`；
- 地头长度不小于 `1.5–2.0 m`，为半径不小于 `0.5 m` 的掉头留空间；
- 用纸箱、泡沫板、交通锥或竖直支架模拟作物边界；
- 雷达安装高度必须能稳定扫到模拟垄，低矮地面标线不能被二维雷达可靠识别；
- 地面标出起点、路径中心线、采样点及固定测量基准点；
- 设置独立物理急停人员，试验区域禁止无关人员进入。

第一次不要加入动态障碍。完成路线重复性验证后，再在直线路段放置一个静态障碍，单独验证停车，不急于验证绕行。

当前 ROS 2/Gazebo 仿真基线已经按上述约束固化在
`spore-patrol-robot-feature-lidar/src/spore_patrol_sim/`：
`config/farmland_layout.json` 是唯一几何参数源，
`worlds/farmland_8x6.world` 是首轮无障碍 SDF 场景，通道采用“净宽”定义而不是
此前小尺度实车试验使用的 1.0 m 垄中心距。仿真场景的离线检查和地图文件验收入口
见 `spore-vehicle-nav/docs/模拟农田建图测试.md`；完成 ROS 2/Gazebo 实际运行前，
不得把离线场景检查写成已完成建图。

## 5. 实施顺序与通过门槛

### Gate A：硬件与安全复核

1. 架空车轮，检查 `/cmd_vel` 正负方向、左右转方向和 0.30 秒看门狗。
2. 落地低速遥控直行、左转、右转和停车。
3. 检查 `/odom`、`/imu/data_calibrated`、`/odometry/filtered`、`/scan` 和 `/diagnostics`。
4. 保持车静止 10 秒完成 IMU 零偏校准。

通过条件：话题持续发布；坐标方向一致；通信断开能停车；物理急停有效。

### Gate B：建图与本地定位

使用仓库已有入口：

```bash
cd ~/spore_patrol_ws
source /opt/ros/humble/setup.bash
source tools/ydlidar_env.sh
source install/setup.bash

ros2 launch spore_patrol_localization mapping_localization.launch.py \
  base_port:=/dev/ttyACM0 \
  lidar_port:=/dev/ydlidar \
  rviz:=true
```

静止完成校准后开始录包：

```bash
tests/slam/record_mapping_bag.sh
```

先以 `0.08 m/s`、`0.20 rad/s` 低速手动绕场一周，保存地图：

```bash
tools/save_slam_map.sh mock_field_01
```

通过条件：地图边界无严重重影；回到起点能重新对齐；`map→odom→base_footprint` TF 连续且唯一；雷达和融合里程计不中断。

说明：第一次自动路径测试也可以只使用 `/odometry/filtered` 的相对定位，避免静态地图定位阻塞验证。但路线短、速度低，并应承认其误差会累计。

### Gate C：路线离线验证

1. 在网页按实测场地尺寸建立边界与垄参数。
2. 导出标准米制路径 JSON。
3. 路线适配器只解析和显示，不发布 `/cmd_vel`。
4. 在 RViz 同时显示地图、机器人足迹、原始路径、平滑路径和采样点。
5. 检查路线是否镜像、越界、穿过模拟垄或地头空间不足。

通过条件：所有采样点和路径位于可通行区域；起点和方向一致；转弯半径不小于 `0.5 m`；相邻重采样点间距建议 `0.10–0.20 m`。

### Gate D：分级自动跟踪

按以下顺序逐级试验，每级至少成功 3 次再升级：

1. `1 m` 直线；
2. `3 m` 直线；
3. 单个缓弯或 90° 大圆弧；
4. 一条垄间直行 + 一个地头 U 形转弯；
5. 两条垄的往复式路线；
6. 完整规划路线 + 采样点停车；
7. 静态障碍进入安全区时停车。

初始参数建议：

| 参数 | 初始值 |
|---|---:|
| 直线速度 | 0.08–0.12 m/s |
| 转弯速度 | 0.05–0.08 m/s |
| 最大角速度 | 0.20–0.25 rad/s |
| 前视距离 | 0.35–0.60 m |
| 普通航点容差 | 0.20 m |
| 采样点容差 | 0.15–0.20 m |
| 采样点稳定时间 | 2 s |
| 横向误差降速阈值 | 0.20 m |
| 横向误差停车阈值 | 0.30 m |

这些值是首轮保守参数，最终根据实车轨迹调节。底盘当前配置上限为 `0.15 m/s` 和 `0.30 rad/s`，验证节点不得绕过该限制。

### Gate E：重复性与故障验证

- 同一路线至少独立运行 5 次；
- 测试中途暂停/继续；
- 路线上触发一次软件停车；
- 断开控制节点，验证底盘看门狗停车；
- 放置静态障碍，验证雷达停车；
- 保存每次 rosbag、路线 JSON、参数、视频和结果表。

## 6. 路线跟踪节点的最小功能

建议新增 `spore_patrol_route_validation` ROS 2 包，第一版只做：

```text
IDLE → VALIDATING → READY → RUNNING
→ DWELLING（采样点停车）→ RUNNING
→ COMPLETED / STOPPED / FAULT
```

输入：

- 路径 JSON 文件；
- `/odometry/filtered`；
- `/scan`；
- 开始、暂停、继续、停止服务。

输出：

- `/cmd_vel`；
- `/validation/path`；
- `/validation/status`；
- `/validation/cross_track_error`；
- `/validation/sample_event`。

必要保护：

- 位姿或雷达数据超时停车；
- 非有限数值、空路径、错误单位或越界路线拒绝启动；
- 横向误差超过阈值停车；
- 障碍距离进入停车区时发布零速度；
- 停止后持续发送短时间零速度，再依靠底盘看门狗兜底。

第一版不实现动态绕行、不接真实采样器、不做多机器人调度。

## 7. 数据记录与评价方法

每次试验都保存：

- 路径 JSON、算法版本和全部运行参数；
- `/cmd_vel`、`/odom`、`/odometry/filtered`、`/scan`、TF、诊断、电池；
- 计划路径、实时状态、横向误差和采样点事件；
- 固定机位全程视频；
- 场地尺寸、通道宽度、模拟垄布置和天气/地面情况；
- 成功、人工干预、碰撞、越界、停车原因。

仅用机器人自己的融合位姿对比计划路线，会低估真实定位误差。为增强方案书可信度，推荐增加独立真值：

- 在车辆顶部贴 ArUco/AprilTag；
- 用高处固定相机拍摄整个小场地；
- 用四个已知场地角点做单应性标定；
- 从视频提取车辆真实二维轨迹，与计划路线对齐计算误差。

若暂时不能自动提取，至少在直线和采样点布置卷尺标线，并用固定俯视视频人工复核。

核心指标：

| 指标 | 计算口径 |
|---|---|
| 路线完成率 | 完成的有效试验次数 / 总有效试验次数 |
| 采样点到达率 | 成功进入容差并稳定停车的采样点 / 总采样点 |
| 横向误差 RMSE | 独立真实位置到计划路径的最短距离 RMSE |
| 横向误差 P95 | 全部真实轨迹横向误差的 95% 分位 |
| 最大横向误差 | 全程最大偏离距离 |
| 停靠误差 | 停稳位置与目标采样点的距离 |
| 路径效率 | 实际行驶距离 / 计划距离 |
| 重复性 | 多次轨迹在同一路段的离散程度 |
| 安全停车距离 | 障碍触发到车辆完全停止的行驶距离 |

阶段性目标可设为：路线完成率 `≥80%`、采样点到达率 `≥90%`、横向误差 P95 `≤0.20 m`、最大误差不超过 `0.30 m`。这些是小型验证目标，不等同于最终企业产品指标。

## 8. 方案书可形成的证据

完成上述试验后，方案书可以严谨地表述：

- 自研路径规划算法已经过 ROS 2 实车闭环验证，而非仅有仿真或界面展示；
- 规划路线可在结构化模拟农田中完成多垄往复式跟踪和采样点停车；
- 已量化路线完成率、横向误差、停靠误差和重复性；
- 雷达、融合里程计、控制看门狗和人工急停构成了验证样机的基本安全链；
- 当前样机证明总体技术路线可行，RTK、环境适应性、真实采样器和产品化可靠性属于后续工程化工作。

不要把模拟农田结果直接写成“大田全场景已达标”，而应说明验证尺度、速度、场地结构、定位来源、测试次数和误差统计。

## 9. 接下来三项具体工作

1. 修正网页路线导出：把所有路段航点转换为统一 `field` 米制坐标，处理 Y 轴方向并补齐路径元数据。
2. 新增轻量 ROS 2 路线验证包：先完成 JSON 解析、RViz 显示和离线校验，再加入 Pure Pursuit 与安全停车。
3. 趁能连接小车，优先完成 Gate A/B 并录制一份基线 rosbag；没有可靠基线数据时不要直接运行完整自动路线。
