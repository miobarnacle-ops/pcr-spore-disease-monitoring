# 机器人车辆导航子系统工作计划与 agent 协作指南

> 项目：基于机器人与 DNA-PCR 技术的孢子病害识别关键技术研究（题目编号 DG-202605，发榜单位：山东祥辰科技集团有限公司，"挑战杯"揭榜挂帅擂台赛）
> 文档用途：本人（车辆部分负责人）持续开发、盘点、测试和记录的主指导文件，供审计与 agent 协作使用
> 当前版本：v0.8.6
> 信息更新日期：2026-08-31（周一）
> 比赛材料提交截止：2026-09-15 前（剩余约 20 天）

---

## 0. 本文档的使用与更新规则

本文档是**活文档**，每次工作后必须同步更新：

1. 状态标记约定：`【已验证】`（有实测证据）、`【部分完成】`、`【进行中】`、`【未开始】`、`【待确认】`（事实未定）。
2. 每次实质进展（测试通过、硬件到货、接口冻结、决策拍板）都要更新对应章节状态，并在第 15 节"更新记录"追加一行。
3. 实测数据必须写明测试日期、场地、原始数据位置，不允许只写"已测试"。
4. 目标值、仿真值、实测值必须分开标注，禁止混写。
5. 每次给 agent（Codex/OpenCode 等）下达任务前，先让其阅读本文档最新版本。

---

## 1. 项目背景和本人的职责边界

竞赛目标：构建面向农田的智能病害监测系统。机器人在电子地图中接受监测区域和路线任务，自主完成巡检、采样点停车、外部采样设备联动及数据回传。

本人只负责**机器人车辆与自主导航子系统**，工作范围：

- 农田卫星图导入、区域框定和牛耕式覆盖路径生成（桌面工具已完成，见 3.2）。
- 规划轨迹及采样点 JSON 的导出、导入、校验和版本管理。
- 图像坐标、农田局部坐标（field/local_enu）和北斗地理坐标之间的转换。
- 树莓派与 R550 PLUS 底盘（C50C/STM32F407 下位机）通信。
- 轮速计、IMU、二维激光雷达、北斗 RTK 的数据接入。
- SLAM、融合定位、自主导航、路径跟踪、转弯和避障。
- 采样点到达、稳定停车、通知外部设备、等待完成和继续行驶的任务状态机。
- 车辆位置、任务进度、速度、电量、定位状态和异常信息回传（经 rosbridge 至 Web 控制台）。
- 定位精度、路径跟踪误差、采样点停车误差、任务成功率和稳定性验证。
- 与车辆部分有关的代码、配置、接口说明、测试数据和演示材料。

以下内容**不在本人职责范围内**：

- PCR 检测原理、核酸提取、扩增和生物实验。
- 引物、探针、病害识别模型和生物数据分析。
- 孢子收集器内部机械结构、气路、电路和采样机理。
- 采样器自身的动作控制算法。

本人只需与采样器负责人共同定义车辆侧接口，并完成联调。

---

## 2. 竞赛官方要求（已从比赛方案 PDF 原文提取）

> 来源：`DG-202605…比赛方案.doc.pdf`（已用 pdftotext 提取全文，2026-08-14 核对）。

### 2.1 与车辆直接相关的要求（原文）

- "结合**北斗+SLAM 融合定位技术（定位误差<0.5 米）**，实现地块级孢子浓度、病害动态的精准监测。"
- "管理人员可在**电子地图划定监测区域，系统自动规划最优巡检路线**，机器人自主完成孢子采集、病害检测、数据上传等任务。"
- "平台通过**高清视频监控实时回传机器人作业动态**，管理人员可通过电脑端、手机端远程查看。"
- "平台构建'**地图路线规划-任务自动执行-异常告警联动**'的闭环体系。"
- 总体要求："方案需**可田间验证**，明确关键参数、性能指标、验证方法"；"所有实验数据、识别准确率、**定位精度等必须真实有效**"。

### 2.2 提交材料清单（与车辆相关部分）

- 核心研究报告（必交，Word+PDF，≥5000 字，封面签字/盖章）。
- 软件与算法成果：算法流程图、系统架构图、数据处理流程图；**可运行代码包（含注释），明确点名"路径规划算法"**；软件界面截图/操作视频；测试数据集说明与结果文件。
- 硬件与装置成果：机器人结构设计图、三维模型；设备实物照片、整机装配图、**现场测试视频**；关键零部件清单与选型说明。
- 实物/原型（鼓励提交，可加分）：可现场演示"孢子采集—样本处理—PCR 扩增—数据回传"全流程。
- 参赛报名表（必交，报名系统审核通过版本）。

### 2.3 命名规范与提交方式

- 总压缩包：`揭榜挂帅-祥辰科技-孢子病害识别-团队名称-联系人电话`
- 报告：`研究报告_Word/PDF_团队名称`；代码：`代码包_算法名称_团队名称`；佐证材料：`佐证材料_类型_团队名称`
- 提交邮箱：方案文中出现 `jnxiangchen@126.com`（第五节点）与 `jnxianchen@126.com`（第八节点）**两种拼写，提交前必须核实**。
- 时间节点：2026-09-15 前提交；09-30 前初审；10 月晋级团队完善作品；11 月终审擂台赛。
- 命名/格式不规范扣 2-5 分；逾期超过 7 天取消资格；提交截止后不接受补报修改。

### 2.4 评分与加分项

- 评分：技术创新性 30 分 + 方案可行性 30 分 + 研究价值 25 分 + 成果完整性 15 分。
- 加分项：**核心技术获专利/论文/软著（+3~5 分）**——团队已有《多模态农田孢子监测与病害扩散预警系统 V1.0》软著申请材料（在揭榜挂帅.zip/软著/）；**完成田间小试验证且效果显著（+2~3 分）**；行业标准草案（+2 分）。
- 发榜单位**免费开放 3 个农业试验基地**（粮食作物、果蔬、中药材），支持田间采样与适配性验证——田间验证应优先申请使用该资源。

### 2.5 需要团队确认的指标

评审说明中"机器人续航 ≥8 h/次"仅为关键参数**示例**，不是官方硬指标。R550 PLUS 标配 24 V 6000 mAh 电池，带载荷连续作业明显达不到 8 h。**需要项目组决定报告中续航指标的表述方式**（如实标注实测值 + 提出换电/扩容方案），不得在报告中虚写。

### 2.6 车辆侧内部验收目标（为比赛指标留余量，非官方原文）

| 指标 | 比赛要求 | 建议内部目标 | 验证方法 |
|---|---:|---:|---|
| 融合定位误差 | `< 0.5 m` | 平面 RMS `<= 0.30 m` | RTK 固定解/独立真值对比 |
| 95% 定位误差 | 未明确 | `<= 0.50 m` | 全程误差分布统计 |
| 路径横向跟踪误差 | 未明确 | RMS `<= 0.20 m`（模拟场地 P95 `<= 0.20 m`） | 规划线与实驶轨迹对比 |
| 采样点停车误差 | 未明确 | `<= 0.30 m` | 已测量采样点重复停车 |
| 自动任务完成率 | 未明确 | 模拟场地阶段 `>= 80%`，最终 `>= 95%` | 多次完整任务统计 |
| 异常时停车 | 远程可控、异常可响应 | 看门狗/失定位/障碍均可可靠停车并记录原因 | 专项测试 |

---

## 3. 项目资产与现状盘点（2026-08-14 基线）

### 3.1 硬件资产

| 项目 | 当前信息 | 状态 |
|---|---|---|
| 车辆平台 | **WHEELTEC R550 PLUS**（高配摆式悬挂四驱底盘） | 【已验证】在手 |
| 下位机 | C50C 控制板（STM32F407VET6），串口通信 | 【已验证】协议已打通 |
| 电机/编码器 | MD36L-P27（24 V、60 W、1:27）+ 500 线 AB 相 GMR 编码器 | 【已验证】 |
| 底盘 IMU | 板载 ICM20948 | 【已验证】数据已接入 |
| 尺寸/质量 | 152 mm 越野轮，图纸轴距 312.7 mm、轮中心距约 335.6 mm，裸车约 7.6 kg | 【已验证】 |
| 电池 | 24 V 6000 mAh | 【已验证】，续航实测未做 |
| 主控计算机 | **树莓派 5（8 GB RAM / 64 GB 存储卡）** | 【已验证】车载 24 V→5 V DC-DC 取电已落地（2026-08-26 确认），不再依赖电池组；Pi 5 需 5 V/5 A，DC-DC 模块留足电流裕量并实测压降正常 |
| 二维激光雷达 | YDLIDAR X3 Pro（CP210x USB 转接板，USB ID `10c4:ea60`） | 【已验证】自购新机已到货，2026-08-20 完成阶段 B 复验（`/scan` 稳定、方向正确、30 分钟零掉线）；udev 固定名 `/dev/ydlidar` 已建 |
| GNSS/北斗模块 | **ATGM336H**，已购 ¥236；`/dev/ttyUSB0`，9600 baud，NMEA，室外已获得经纬度 | 【已验证基础链路】普通单频单点 GNSS；不支持 RTK Float/Fix、RTCM 差分、双天线定向；不足以直接证明 `<0.5 m` |
| 说明书资料包 | `轮趣R550 PLUS/`（R550 PLUS 系列资料，C50C 版源码与开发手册） | 【已验证】在本地 |
| 硬件预算 | **3000 元专项用于北斗 RTK 链路**（接收机/天线/4G 流量等；雷达已另行购买不占此预算） | 【已确认 2026-08-14】 |
| 测试场地 | 【已确认暂定 2026-08-14】**学校小花园**，轻量化模拟验证（不进真实农田）；模拟垄/采样点在小花园内搭建；发榜单位 3 个免费试验基地保留为后续可选加分项 | 【已确认】 |

### 3.2 软件资产与仓库

| 资产 | 位置 | 状态 |
|---|---|---|
| ROS 2 机器人工作区（主仓库） | GitHub `BrownSun250912/spore-patrol-robot`（公开）；本地 `spore-patrol-robot/`，分支 `feature/miobarnacle` | 【已验证】PC 端 `~/spore_patrol_ws` 可编译运行 |
| 雷达/SLAM 成果分支 | 本地 `spore-patrol-robot-feature-lidar/`（含 `spore_patrol_lidar`、`spore_patrol_slam`、`spore_patrol_localization`、ydlidar 驱动、maps、tools） | 【仿真首轮已验证】软件与自购 X3 Pro 已完成阶段 B 复验；canonical 模拟农田已完成 ROS 2/Gazebo 建图与地图几何验收；实机/小花园和 rosbag 仍待阶段 C |
| 桌面路径规划工具 | 揭榜挂帅.zip 内 `软著/agri_path_planner.py`（tkinter GUI） | 【已验证】功能完成，见下 |
| Web 控制台 | 本地 `spore-monitor-web/`（Next.js 16 + React 19 + roslib + drizzle） | 【基础实时链路已验证】7 页签与路线导出可用；机器人监控和统一任务/遥测面板可经 rosbridge 读取融合位姿、雷达、电池、诊断、车端任务状态和可选 GNSS；采样器/视频实机适配仍待完成 |
| 底盘串口测试工具 | `spore-patrol-robot/tests/chassis_serial/`（含伪串口模拟器和离线单测） | 【已验证】 |
| 整合与验证方案 | `INTEGRATION_PLAN.md`、`NAVIGATION_VALIDATION_PLAN.md`（项目根目录） | 已评审，作为本计划的配套文件 |
| 车辆导航代码归集仓库 | 本地 `spore-vehicle-nav/`（colcon 工作区，git 本地仓；含 `spore_patrol_route_validation` 阶段 D 与 `spore_patrol_nav_bringup` 阶段 C） | 【状态同步 2026-08-31】阶段 C 已形成并实际运行仿真建图/保存/验收链；正式仿真地图归档在雷达/SLAM 分支 `tests/results/`，实机/小花园地图与 rosbag 尚未归档。阶段 D 包已实现，当前 91 项离线 pytest 通过；小尺度 6 类 9 次实车试验已记录通过，但每级×3、独立真值 P95 与小花园正式路线仍待补齐。 |

**桌面路径规划工具功能**（与用户描述一致，已核对源码）：

- 导入农田卫星照片，画布上框定不规则多边形边界；
- 输入参考边实际长度（建立像素→米比例）、垄间距、每轮采样点数、采样半径、轮数、车速；
- 生成牛耕式（往复式）覆盖路径 + 采样点贪心选址 + TSP 巡回排序；
- 导出 CSV 和 JSON（格式见 5.4）。

**Web 控制台机器人相关功能**：

- "路径规划"页签：边界编辑、采样点、牛耕式路线，导出标准米制 `route_v1.json`（schema 见 `spore-monitor-web/ROUTE_SCHEMA.md`，有自动化测试 `npm run test:route`）；
- "机器人监控"页签：经 rosbridge 优先订阅 `/odometry/filtered`（EKF 不可用时回退 `/odom`）、`/scan`、电池与诊断，支持轨迹显示/回放，运动控制默认锁定；
- 其余页签（环境/PCR/病害/预警/监管）不属于本人职责，但车辆状态机会向其提供任务与位置数据。

### 3.3 已验证的能力（PC 端与车辆端，按条目标注）

以下来自仓库 README、dev_log（2026-07-26/27）与 tests 记录：

- STM32 串口协议：11 字节速度命令 + 24 字节状态反馈，状态反馈约 20 Hz；
- ROS 2 底盘驱动：`/cmd_vel` 控制、轮式 `/odom`、`odom -> base_footprint` TF、`/imu/data_raw`、`/battery_state`、`/diagnostics`；
- 安全链：上位机 0.30 s 命令看门狗、速度/加速度限制、低电压诊断、串口断线重连；STM32 通信失联停车曾有一次通过记录，但 2026-08-20 严格复测 1.309 s/1.409 s 超过 1.2 s 阈值，当前按 FAIL 处理（见 §11.1、§12）；
- 实车运动：3 m 直线（编码器 3.000 m ≈ 卷尺 3.05 m）、90°/360° 原地旋转正常；
- X3 Pro（7 月实物）：固定版本 SDK + 官方 ROS 2 驱动、udev 固定设备名 `/dev/ydlidar`、`/scan` 发布（30 s 实测约 12 Hz、每圈约 340 点）、方向经实物箱体校正（`reversion=true`、`inverted=true`）、RViz 联调；
- SLAM：SLAM Toolbox 异步建图参数、录包脚本、地图/位姿图保存工具；
- 本地融合：`robot_localization` EKF（轮速平移 + ICM20948 偏航角速度），TF 唯一发布者已理顺（`map->odom->base_footprint->base_link->laser_link`）；
- GNSS 适配：已完成 `spore_patrol_gnss` ROS 2 包的离线实现（NMEA GGA/GLL/VTG、`/fix`、速度、原始数据、诊断、可选 ENU）；实机串口与融合效果仍待上车复验；
- 仿真：Gazebo 简化农田场景 + 二维激光仿真、URDF/Xacro 整车模型。

**2026-08-26 车辆导航阶段 C/D 新增实测：**

- 阶段 C 基础组合栈：树莓派上确认底盘、雷达、EKF、SLAM Toolbox 同时运行，`/odometry/filtered` 约 20 Hz、`/scan` 约 11.45 Hz，`map→odom→base_footprint` TF 链已检查；这只证明基础栈启动与数据链路，不等同于已完成地图验收；
- 阶段 D 小尺度实车：1 m 直线、3 m 直线、方形回环、双垄往复、完整多点采样路线、静态障碍停车均有记录，§11.1 汇总为 6 类 9 次通过；正式横向误差仍缺独立真值统计。

**2026-08-31 模拟农田建图新增验证：**

- 在独立仿真 ROS 域 `42` 中实际运行 ROS 2 Humble + Gazebo Classic + SLAM Toolbox；无 GUI 启动后 `/scan`、`/odom`、`/map` 均可用，仿真进程组退出后无残留；
- 正式低速固定闭环的 9 个 `/odom` 反馈航点全部到达，地图 YAML/PGM 与 pose graph/data 保存成功；通用地图验收通过，网格覆盖约 7.85×5.85 m、分辨率 0.05 m/cell；
- 三垄几何验收通过：三条垄覆盖率 100%、100%、100%，最大轨迹离散约 0.030 m；曾复现旧宽松回环阈值导致的弯曲重影，已收紧正式 `sim_mapping.yaml`，旧失败结果不作为合格地图；
- 该结果只证明仿真场景与建图软件链，不等同于实机/小花园地图、真实 rosbag、独立真值或 `<0.5 m` 比赛精度。

**2026-08-30 实机恢复与静止验收：**

- 电脑 `<PRIVATE_IP>` 与树莓派 `<VEHICLE_HOST> (<PRIVATE_IP>)` 同网段，Ping/SSH 正常；树莓派上 `/dev/ttyACM0`（C50C）与 `/dev/ttyUSB0`（X3 Pro）均被内核识别；
- 修复 `spore_patrol_nav_bringup` 在 ROS 2 Humble 中错误导入 `FindPackageShare` 的启动错误；新增 `full_stack.launch.py`，统一拉起底盘反馈、YDLIDAR、EKF、SLAM Toolbox；
- 修复 Docker 容器内缺少 `/dev/ydlidar` 别名，并安装/启用 `spore-vehicle-stack.service` 与 `spore-rosbridge.service`，容器重启演练后自动恢复且无重复活动节点；
- 静止只读验证：`/odom` 与 `/imu/data_raw` 约 20 Hz、`/odometry/filtered` 约 20 Hz、`/scan` 约 11.3 Hz，底盘反馈约 20 Hz、电池约 23.7 V，TF 链完整；电脑端 rosbridge 真实订阅收到融合里程计/雷达/电池/诊断数据；
- 本次未启动路线跟踪、未发布非零 `/cmd_vel`，因此不构成运动、建图质量、跟踪误差、failsafe 或 `<0.5 m` 精度验收；
- 已将 `spore_patrol_gnss` 部署到树莓派并完成 `enabled=false` 的 ROS 2 launch 检查；当前 `/dev/ttyUSB0` 是 X3 Pro 雷达，ATGM336H 待确认专用设备别名后再启用，避免误读雷达串口；

**2026-08-15 凌晨新增（PC+小车 USB 直连，8 小时无人值守，详见 §11 测试记录）**：

- 真实底盘 8 小时连续只读监听 20Hz 状态帧无中断（电池 23.82~24.13V 区间正常）；
- 架空低速运动 vx=0.05 m/s×2s 共 48 轮全部成功，每轮运动后均正确回零（vx/vy/wz=0.000），异常中断 0 次；
- 确认测试工具内置二次安全机制：`motion/sequence` 必须带 `--arm WHEELS_OFF_GROUND` 才执行，`failsafe` 需 `--arm WHEELS_OFF_GROUND_AND_EMERGENCY_STOP_READY`（不给参数直接拒绝，属第二道防线）；

### 3.4 尚未完成（车辆侧）

- 树莓派部署：系统、ROS 2、工作区迁移、车载 24 V→5 V DC-DC 供电、udev 固定设备名——**已完成**（见 §7 阶段 A）；仅 PC 端 RViz 远程联调遗留；
- 自购 X3 Pro 复验**已完成**（2026-08-20，阶段 B）；室外强光/植株环境评估**未开始**；
- 第一张室内/模拟农田二维地图及回环/重复建图误差评估——**canonical 模拟农田首轮地图已实际生成并通过结构/三垄几何验收，结果在 `spore-patrol-robot-feature-lidar/tests/results/20260831_135053_sim_farmland_8x6/`；`spore-vehicle-nav/maps/` 仍只有占位文件，实机/小花园地图、重复路线统计和 rosbag 尚未完成**；
- EKF 实车效果定量验证——**未完成**；当前只确认 `/odometry/filtered` 发布和 TF 链路，尚无与独立真值对比的误差统计；
- 路径 JSON 车端适配节点与 Pure Pursuit 路径跟踪——`spore_patrol_route_validation` **已实现，当前 91 项离线 pytest 通过，并已完成小尺度实车闭环**；正式小花园路线、每级重复性和独立真值误差仍待补齐；
- Nav2 静态地图定位与动态避障——未开始；
- 普通 GNSS 实机 ROS 2 接入——成员已完成 ROS 1 Noetic 原型，ROS 2 适配包已部署并完成禁用串口的启动检查；仍待接入 ATGM336H 专用串口后做 NMEA/`/fix` 联调；
- 北斗 RTK 全链路（多频接收机、CORS/NTRIP、RTK 状态、全局融合）——**未启动**；当前 ATGM336H 不具备该能力；
- "到点—停车—采样—确认—继续"软件状态机（2 s 模拟采样）已实现，并在双垄/完整路线中实测过状态序列；真实采样器接口联调——未开始；
- 定位误差、跟踪误差、停车误差的独立真值测量——未开始；
- 载荷续航、整机集成与田间验证——未开始。

---

## 4. 事实澄清与更正（相对 v0.1，审计重点）

| # | v0.1 表述 | 2026-08-14 核实结果 |
|---|---|---|
| 1 | "R680 ROS 小车" | **实为 WHEELTEC R550 PLUS**。误会来源：说明书包内层文件夹名为"2.WHEELTEC R680 ROS大型科研机器人系列底盘资料"，但其内《资料文件结构.txt》明确写的是"R550 PLUS ROS大型科研机器人系列资料"。控制器为 C50C。本文档统一使用 R550 PLUS。 |
| 2 | "ROS 版本未知，禁止提前假定" | **已确定 ROS 2 Humble**（团队仓库与全部已有代码均为 Humble，7 月已实车运行）。 |
| 3 | "底盘通信方式未检查、实车接口未盘点" | **已完成**：USB 串口（C50C）+ 自定义二进制协议，ROS 2 驱动与安全机制已实车验证（见 3.3）。v0.1 的阶段 0/阶段 1 大部分已过时。 |
| 4 | "X3 PRO 已选型，但暂时不在手边" | 7 月已用实物打通全部软件链路并有实测数据；该机目前在项目其他成员手中。**自购新机已下单，预计 2026-08-18 到货**，到货后需复验。 |
| 5 | "JSON 格式尚未提供示例、待确认" | 现有**两种** JSON：桌面工具版（rounds/relative_m，见 5.4-A）与 Web 控制台版（route_v1.json/local_enu，见 5.4-B）。统一决策见决策 D1。 |
| 6 | "主控：树莓派 5 已确认" | **树莓派 5（8 GB）已确认**；存储容量与车载供电方案待确认。此前全部开发在 PC（x86 Ubuntu）完成，树莓派部署是新增任务。 |
| 7 | "路径规划程序待接入实车" | 补充事实：桌面工具算法完成；Web 端路线 schema 与校验已完成；缺的是**车端执行链路**（适配节点 + 跟踪 + 任务状态机）。 |

---

## 5. 目标系统架构（树莓派车载）

### 5.1 硬件连接

```text
树莓派（ROS 2 Humble）
  ├─ USB ── CP210x ── C50C 下位机（STM32F407：电机/编码器/ICM20948/电池）
  ├─ USB ── CP210x ── YDLIDAR X3 Pro
  ├─ USB/UART ── ATGM336H 普通 GNSS（`/dev/ttyUSB0`，9600 baud）
  ├─ 网络 ── 路由器/热点 ── 运行 Web 控制台的电脑/手机
  └─ （采样器接口由整机集成决定：串口/网络/IO）
```

注意：底盘与雷达都是 CP210x，**必须配置 udev 按 USB ID/序列号区分固定设备名**，禁止依赖 `ttyUSB0/1` 顺序。

### 5.2 软件架构

```text
Web 控制台（spore-monitor-web）
  └─ rosbridge ──┐
                 ▼
任务适配层（JSON 校验/坐标转换/航点生成）
  → 任务状态机（到点-停车-采样-继续）→ 路径跟踪（Pure Pursuit，后续升级 Nav2）
  → spore_patrol_base_driver → C50C
传感器链：X3 Pro → /scan → SLAM Toolbox；C50C → /odom + /imu → EKF
全局链：普通 GNSS → /fix + `single` 状态 →（待实机验证的）局部 ENU/融合；RTK 全局链仍待另行选型
```

部署形态：比赛阶段采用"车载树莓派 + 局域网路由器/4G + 浏览器"边缘部署，外网中断不影响巡检与缓存（沿用 INTEGRATION_PLAN 结论）。

### 5.3 坐标系与 TF

沿用仓库已理顺的链：`map -> odom -> base_footprint -> base_link -> laser_link`，新增 `gnss_link`。TF 唯一发布者规则不变（SLAM 发 `map->odom`，EKF 发 `odom->base_footprint`）。田间任务增加 `field`（田块 local_enu）与 `map` 的静态变换，原点与轴向按 ROUTE_SCHEMA 约定（第一个边界点为原点、第一边界边为 +X、田块内部为 +Y 侧）。

### 5.4 两种现有路径 JSON 与统一方向

**A. 桌面工具导出**（`agri_path_planner.py`，已核对 export_json 源码）：

```json
{
  "meta": {"version": "1.0", "scale_px_per_m": 50.0, "reference_point": {...}, "field_params": {...}, "coverage_summary": {...}, "num_rounds": 2},
  "field_polygon": {"vertices_m": [[x, y], ...], "vertices_px": [...]},
  "rounds": [{"round_id": 1, "waypoints": [{"id": "R1-P1", "pixel": [..], "relative_m": [x, y], "ridge_index": 0}], "segments": [...]}]
}
```

特点：以多边形第一标定点为原点的相对米制坐标；**只有采样点/路段，没有完整行驶路径点序列，无航点类型、速度、容差字段，无 Y 轴方向约定**。

**B. Web 控制台导出**（`route_v1.json`，`ROUTE_SCHEMA.md`）：

```json
{
  "schema_version": "1.0", "frame_id": "field", "coordinate_type": "local_enu", "unit": "m",
  "field_polygon_m": [...], "sampling_points": [...],
  "path": [{"seq": 0, "x_m": 0, "y_m": 0, "yaw_rad": null, "type": "sampling", "round": 1, "sample_id": "R1-P1", "speed_limit_mps": 0.12, "tolerance_m": 0.2}]
}
```

特点：完整路径点序列、字段齐全、有坐标方向约定、有校验器和自动化测试，且是 NAVIGATION_VALIDATION_PLAN 与 INTEGRATION_PLAN 选定的整合方向。

**统一方向（决策 D1）**：车端标准任务包采用 **B（route_v1.json）**；桌面工具 A 的结果通过一次离线转换进入 B（或后续在桌面工具中增加同款导出），避免车端维护两套解析器。A 的覆盖评估/TSP 算法成果继续作为报告与软著材料保留。【已冻结 2026-08-14：用户确认同意】

---

## 6. 采样设备联动边界（车辆侧状态机）

```text
IDLE → TASK_LOADED → NAVIGATING → ARRIVED → SETTLING
→ SAMPLE_REQUESTED → SAMPLING → SAMPLE_FINISHED → NAVIGATING / TASK_FINISHED
（任意状态 → STOPPED / FAULT，均需可恢复与日志）
```

车辆侧负责：发布 `task_id`、`sample_id`、位置、时间戳、定位质量；到点停车并确认稳定；发"允许采样"请求；等待成功/失败/超时；记录事件日志并决定继续、重试或终止。

采样设备侧负责：执行内部动作；返回接收/忙碌/完成/失败与故障码。

接口未定前先用**模拟节点**开发状态机（NAVIGATION_VALIDATION_PLAN 中采样动作先以"停车 2 s 并记录事件"代替），不绑定具体通信实现。

---

## 7. 分阶段实施计划（按 2026-08-14 现状重排）

> 原则：先打通最小可演示闭环（路径执行 + 停车 + 回传），再补 RTK 精度与联动；RTK 采购是关键路径，必须立即启动。

### 阶段 A：树莓派部署与底盘闭环迁移（08-14 ~ 08-17）【基础部署与自动恢复已完成，现场专项仍有遗留】

> 操作指南：`树莓派5部署清单.md`（§0-1 部署实录）。
> **D2 已再修订**：Ubuntu 22.04 镜像不支持 Pi 5，实际部署为 **Ubuntu Server 24.04.4 + Docker（ros:humble-ros-base 容器，host 网络/privileged/设备直通，ROS_DOMAIN_ID=77）**。

- [x] 系统烧录（24.04.4，清华镜像，SHA256 校验）；SSH 0 字节主机密钥问题已修复；
- [x] Docker 29 + 国内镜像加速 + ros:humble-ros-base；
- [x] 工作区迁移至 `/home/pi/spore_patrol_ws`（容器内 `/ws`）；
- [x] YDLidar-SDK（arm64）编译安装 + 8 包 colcon build 全部通过（2026-08-14）；
- [x] 容器易用性：`humble` 别名进入即自动 source；
- [x] 车载供电：小车 24 V→5 V DC-DC 模块已采购并上车，2026-08-26 确认供电稳定（压降正常，Pi 5 不再依赖电池组）；
- [x] udev 固定设备名：雷达 CP210x→`/dev/ydlidar`、底盘 CH340→`/dev/ttyACM0` 已建，用户已加入 `dialout` 组；
- [x] 车端完整基础栈入口与自动恢复：`full_stack.launch.py` + `spore-vehicle-stack.service`，底盘/雷达/EKF/SLAM 在容器重启后可自动恢复；
- [x] rosbridge 实时网关：`spore-rosbridge.service` 固定监听 9091，电脑端真实 WebSocket 订阅验证通过；
- [x] 底盘串口链路验证【2026-08-15 凌晨】：CH340 → `/dev/ttyACM0`，20 Hz 状态帧、电池 24.1V、协议解码正常；
- [x] 架空低速运动测试【2026-08-15 凌晨】：`--arm WHEELS_OFF_GROUND`，vx=0.05 m/s×2s，运动后回零；**48 轮 8 小时稳定性通过**（详见 §11.1）；
- [ ] 失联停车测试（failsafe）【2026-08-20 复测 2 次 FAIL：1.309s / 1.409s 超 1.2s 阈值；2026-08-26 仍**未处理**，维持 FAIL 判定。车轮视频确认停稳，但固件失联响应系统性偏慢；ROS 驱动层 `cmd_vel_timeout_s: 0.30` 机制待验证。风险见 §12】；
- [ ] PC 端 RViz 远程联调（PC 已有 ROS 2 Humble，可用 rosbridge/同网联调）；

完成判据：树莓派单独控制小车完成与 PC 端一致的只读 + 低速运动测试，日志齐全。

### 阶段 B：自购 X3 Pro 到货验证（预计 08-18 ~ 08-21，依赖到货）【已完成 08-20】

- [x] 开箱核对配件（USB 转接板、线缆）；`tests/lidar/lidar_test.py` 端口与数据质量检查【08-20：CP210x(10c4:ea60)→/dev/ttyUSB0，udev 固定名 /dev/ydlidar 已建】；
- [x] 树莓派上发布 `/scan`【08-20：11.37Hz，pass:true；数据：360点/圈、有效254、范围0.16~5.7m】，复核 `reversion`/`inverted`【08-20：前左箱子实测出现在 FL 象限，方向正确】；
- [x] TF/RViz 显示：`frame_id=laser_link` 正确（RViz 实机显示待补截图）；
- [x] 静止 30 分钟连续运行掉线测试【08-20 已完成，**通过**：11.45Hz、360点/圈、有效244点(67.8%)、0.12~7.98m、20580帧零掉线，pass:true】；低速行驶运动畸变观察【待做】；
- [x] 与 7 月数据对比【08-20 已完成：频率11.45Hz(vs 7月~12Hz)、点数360(vs ~340)、有效244(vs ~199)、测距0.12~7.98m，质量一致且略有提升】，记录差异。

完成判据：树莓派上 `/scan` 稳定，方向正确，连续运行无不可恢复掉线。

### 阶段 C：EKF 实车验证与建图（计划 08-20 ~ 08-26；**仿真首轮建图已完成，实机/现场尚未完成**）【基础栈已实测·仿真链已验证·现场建图待完成】

- [x] 按官方流程在树莓派上跑通"底盘 + 雷达 + EKF + SLAM Toolbox"组合启动【2026-08-26 首轮实测；2026-08-30 修复启动错误并完成 systemd/容器重启复验：`/odometry/filtered` 约 20 Hz、`/scan` 约 11.3 Hz，`map→odom→base_footprint` TF 链已检查；详见 §3.3、§11.1 与 §15】；
- [x] 远程建立并运行模拟农田建图基线【2026-08-31】：在当前雷达/SLAM 分支新增并实际运行 8 m × 6 m、3 条模拟垄、1.20 m 净通道、1.60 m 地头的 canonical SDF 场景；无 GUI 启动检查、9 航点低速闭环、地图/pose graph 保存、通用栅格验收和三垄几何验收均通过。正式配置的回环保持开启，并已收紧重复垄道场景的匹配阈值；详见 `spore-vehicle-nav/docs/模拟农田建图测试.md`；
- [x] canonical 仿真重复建图一致性【2026-08-31】：两次同配置低速固定闭环的地图均通过三垄几何验收；`compare_farmland_maps.py` 比较三条垄中心偏移均为 0.050 m，低于 0.150 m 门槛；
- [ ] 完成第一张室内/实机场景地图，检查重影、回环、TF 连续性；保存至 `maps/`【当前 `spore-vehicle-nav/maps/` 仍只有 `.gitkeep`；仿真地图归档在雷达/SLAM 分支 `tests/results/20260831_135053_sim_farmland_8x6/`】；
- [ ] **在学校小花园内**搭建 8 m × 6 m 级模拟农田（2–3 条模拟垄、通道 1.2 m、地头 1.5–2 m，按 NAVIGATION_VALIDATION_PLAN §4；提前与学校确认场地许可）；
- [ ] 实机模拟农田建图与重复路线一致性测试；保存 rosbag【仿真首轮固定闭环已完成，重复运行统计和实机 bag 仍待补齐】。

阶段性结果：基础组合栈已在树莓派运行；模拟农田已完成 ROS 2/Gazebo 实际启动、固定闭环、地图/pose graph 保存、三垄几何验收和两次重复地图一致性比较。尚未完成的是室内/小花园实机地图、实机重复路线 rosbag 与独立真值。

正式完成判据（尚未满足）：室内与模拟农田均有可用地图；`map→odom→base_footprint` 连续唯一；短时扫描退化时里程计连续。

### 阶段 D：路径 JSON 接入与路径跟踪验证（计划 08-24 ~ 08-31，与 C 交叉；**代码已实现（当前 91 项离线 pytest 通过）+ 小尺度实车分级已记录通过**）【小尺度闭环已通过·统计/场地证据待补】

按 NAVIGATION_VALIDATION_PLAN 的 Gate C/D/E 执行：

- [x] `spore_patrol_route_validation` 包已实现：解析 route_v1.json → field→map 转换 → 平滑/重采样 → Pure Pursuit → `/cmd_vel`；当前 91 项离线 pytest 通过；
- [x] 离线逻辑校验：路线 schema、field→map 变换、平滑/重采样、Pure Pursuit、采样状态机和安全判定均有回归测试；
- [ ] RViz 叠加地图、路径、足迹的现场视觉核验与独立真值对照仍待补齐；
- [x] 小尺度实测已完成并记录通过：1 m 直线、3 m 直线、方形回环、双垄往复、完整多点采样路线、静态障碍停车，§11.1 汇总为 6 类 9 次；缓弯/单垄场景需在小花园模拟农田中单独补测；
- [ ] 每级至少成功 3 次再升级；当前已有 3 m 直线×3、双垄往复×2 等记录，但尚未满足所有等级均×3；初始速度沿用底盘限制（0.15 m/s、0.30 rad/s）；
- [x] 雷达前向 ±90° 静态障碍停车专项已通过，包含“行驶→刹停→锁定→移障恢复”证据链；
- [ ] 同一路线重复 5 次、暂停/继续、看门狗专项及全程录包/固定机位视频尚未形成完整实车证据。

阶段性结果：小尺度路线执行、采样 2 s 模拟驻留、状态序列和雷达停车已实车闭环。

正式完成判据（尚未全部满足）：完整牛耕式路线实车闭环；路线完成率 ≥80%、采样点到达率 ≥90%、横向误差 P95 ≤0.20 m（模拟场地阶段目标）；并需保留独立真值、重复性和全程记录。

### 阶段 E：北斗 RTK 接入与全局融合（预计 08-18 启动选型采购，09-01 ~ 09-07 接入）

- [ ] **立即**（08-18 前）：确认测试地 CORS/NTRIP 可用性与账号；在预算内完成选型（北斗多频、RTK 固定解、NMEA/RTCM 输出、ROS 可接入）并下单；
- [ ] 发布 `/fix`、`/gps/rtk_status`（Single/Float/Fix）；
- [ ] 建立经纬度↔田块 field/ENU 转换（WGS84/CGCS2000 基准、原点、X 轴航向写入任务包）；
- [ ] 天线相位中心到 `base_link` 外参测量；RTK 解状态纳入定位质量与降级策略；
- [ ] 静态、直线、闭环、重复停车精度测试；与独立真值（卷尺基准点/俯视相机单应性）对比；
- [ ] 输出定位误差统计（RMSE、P95、最大误差），对照 <0.5 m 指标。

完成判据：独立真值方法证明定位误差满足比赛要求；原始数据与统计脚本可追溯。

### 阶段 F：采样联动与控制台闭环（预计 09-04 ~ 09-10）

- [ ] 与采样器负责人冻结接口字段、超时与故障码；
- [ ] 模拟采样节点跑通任务状态机全流程；
- [ ] Web 控制台：任务下发、进度、RTK 状态、电池/诊断、告警显示；`/odometry/filtered` 替代 `/odom`；
- [ ] 断联、设备忙、采样失败、人工取消测试；
- [ ] 录制完整演示视频（规划 → 下发 → 自动巡检 → 到点采样 → 数据回传）。

完成判据：不接真实采样器可完成全流程演示；接真实设备后无重复采样；每个采样事件有地理位置与时间戳。

### 阶段 G：田间验证与比赛材料（预计 09-08 ~ 09-15）

- [ ] 冻结代码版本、参数、地图与任务 JSON；
- [ ] 视情况申请发榜单位试验基地做一次真实田间小试（加分项 +2~3）；**主验证场地为校内小花园模拟农田，报告中必须如实标注验证尺度、场地结构、速度与定位来源，不得写成大田全场景达标**；
- [ ] 整理：算法流程图、系统架构图、接口说明、测试报告、原始数据（rosbag/GNSS/视频/统计表）；
- [ ] 路径规划代码包按"代码包_算法名称_团队名称"规范整理（含注释）；
- [ ] 核实提交邮箱拼写与命名规范，09-15 前完成提交并留存发送凭证。

---

## 8. 截止倒排时间表（2026-08-14 → 09-15）

| 日期 | 主要目标 | 必须形成的结果 |
|---|---|---|
| 08-14 ~ 08-17 | 树莓派部署（阶段 A） | Pi 上底盘闭环复现 |
| 08-18 前 | **RTK 选型下单 + CORS 确认**（关键路径） | 订单与 CORS 账号结论 |
| 08-18 ~ 08-21 | 自购雷达复验（阶段 B） | `/scan` 在 Pi 上稳定 |
| 08-20 ~ 08-26 | EKF + 建图（阶段 C） | **基础组合栈已在树莓派启动检查；2026-08-31 canonical 仿真农田地图已生成并通过结构/几何验收**；室内/小花园实机地图、重复路线和 rosbag 待补 |
| 08-24 ~ 08-31 | 路径跟踪（阶段 D） | **小尺度 6 类 9 次实车试验已记录通过**；每级×3、独立真值误差和小花园正式路线待补 |
| 09-01 ~ 09-07 | RTK 接入与融合（阶段 E） | 定位误差统计满足 <0.5 m |
| 09-04 ~ 09-10 | 采样联动 + 控制台闭环（阶段 F） | 全流程演示视频 |
| 09-08 ~ 09-13 | 田间小试 + 指标补测（阶段 G） | 田间证据、误差统计 |
| 09-14 ~ 09-15 | 冻结与提交 | 全部材料按规范命名提交 |

> ⚠️ **状态同步（2026-08-31）**：阶段 A 的车载基础栈、容器重启自动恢复和 rosbridge 实时数据链路已修复并完成静止验收；统一任务/遥测 rosbridge 适配和 GNSS 包的禁用串口部署检查已完成；阶段 C 的 canonical 仿真农田已实际完成启动、固定闭环、地图/pose graph 保存和三垄几何验收，但室内/小花园实机地图、重复路线与 rosbag 尚未完成；阶段 D 代码当前 91 项 pytest 通过，小尺度 6 类 9 次实车试验已记录通过。后续重点是小花园建图/路线、每级×3补齐、独立真值 P95、重复/暂停/看门狗专项以及阶段 E 的 RTK 决策；不得把仿真地图、静止验收、小尺度路线通过或普通 GNSS 适配包当作 `<0.5 m` 证据。

降级预案（硬件延误时）：用录制数据验证 SLAM/融合；用模拟采样节点验证状态机；小范围实车路线证明路径接入；报告中明确区分"已实测"与"设计目标"。

---

## 9. 当前立即执行的任务（优先级排序）

> 2026-08-31 更新：阶段 A/B 已完成，且车载基础栈、自动恢复与 rosbridge 已完成静止复验；阶段 C **canonical 仿真农田首轮建图已通过，但实机/小花园地图与 rosbag 未完成**；阶段 D **六类小尺度分级试验已记录通过**（见 §11.1），但正式统计证据尚未收口。剩余工作按三个窗口推进：

**窗口一（08-27 ~ 09-02）P0 两件不可再拖的事**
1. **RTK 选型下单【硬期限 09-03】**：附录 A 清单当日发负责成员拍板；同日产出 ¥3000 内候选方案对比表（北斗双频/固定解/NMEA 三硬条件）；并行确认 CORS/NTRIP 可用性。产出=订单+CORS 结论。
2. **推进实机模拟农田建图**：仿真基线已在 `spore-patrol-robot-feature-lidar/tests/results/20260831_135053_sim_farmland_8x6/` 形成；下一步报备占用时段 → 搭模拟农田（8m×6m、3 垄、**净通道 1.20m**、地头 1.60m）→ 实机只读建图并落盘（serialize_map + map_saver，地图与 rosbag 进入 `maps/`/`results/`）→ 在该图上跑完整牛耕式路线。
3. 补齐：分级试验每级第 3 次 + 规范化录包命名。

**窗口二（09-03 ~ 09-09）P1 精度取证 + 业务闭环**
4. RTK 到货接入：NMEA→`/fix`→ENU 转换→全局融合→静态/直线/闭环精度实测（对照 <0.5 m）。
5. 独立真值装置：车顶 ArUco + 高处俯拍/卷尺基准——**当前误差均为里程计自证，报告级数据必须用它重测**。
6. 采样器接口冻结碰头会（字段/超时/故障码）→ 状态机对接模拟节点。

**窗口三（09-10 ~ 09-15）P2 材料冲刺**
7. 全链路演示视频（规划→下发→巡检→到点采样→回传一条过）。
8. 研究报告 ≥5000 字 + 流程图×3 + 架构图 + 代码包规范重命名 + 测试数据说明。
9. 留 1 天缓冲核对邮箱拼写与命名规范。

风险预案：RTK 物流 3–7 天，下单每晚一天都压缩接入窗口；CORS 不可用需自建基站（成本翻倍），须本周由组长拍板；雨天回退至当前空旷场地录制演示素材。历史事项状态：~~供电方案~~/~~硬件采购~~均已于 08-26 落地完毕。

---

## 10. 关键决策门

### D1：车端任务 JSON 标准【已冻结 2026-08-14】
车端标准任务包采用 Web 控制台 `route_v1.json`（local_enu、字段齐全、有校验与测试）；桌面规划工具结果经离线转换进入该格式。schema 自即日起冻结，车端与 Web 端不得单方面修改字段；如需变更须双方确认并升版本号。

### D2：树莓派系统与部署形态【已拍板，实际部署复核 2026-08-30】
原计划为 Ubuntu Server 22.04 LTS（arm64）原生 ROS 2 Humble；但由于 Pi 5 对当时 22.04 镜像设备树支持不足，实际落地采用 **Ubuntu Server 24.04.4 + Docker `ros:humble-ros-base`**，容器使用 host 网络、privileged 和工作区挂载 `/home/pi/spore_patrol_ws→/ws`，`ROS_DOMAIN_ID=77`。当前以 `spore-vehicle-stack.service`、`spore-rosbridge.service` 负责自动恢复；不得再按原生 22.04 流程重装。硬件：Pi 5 / 8 GB / 64 GB 卡，车载 24 V→5 V DC-DC 已落地。操作手册：`树莓派5部署清单.md`。

### D3：北斗 RTK 方案【阶段性结论 2026-08-30】
预算已确认为 3000 元专项（不含雷达）。已购 ATGM336H（¥236）只能提供普通单频单点 GNSS：串口/NMEA 与 ROS 1 Noetic 原型已打通，室外获得过经纬度；不支持 RTK Float/Fix、RTCM 差分、CORS/NTRIP 或双天线定向。因此它可作为普通 GNSS 降级/初始位置来源，但不能单独满足或证明 `<0.5 m`。剩余 RTK 选型仍需由项目组决定，必须确认多频固定解、差分链路、测量型天线、ROS 2 接入和独立真值测试。

### D4：续航指标表述
R550 PLUS 实测续航必然远低于 8 h 示例值。需要项目组决定：如实报告实测值 + 换电/扩容方案，或采购大容量电池。禁止虚写。
### D5：路径跟踪技术路线

第一版用轻量 Pure Pursuit 自建节点（边界清楚、开发量可控）；Nav2 作为后续增强用于证明动态避障能力，不阻塞首次路线试验（沿用 NAVIGATION_VALIDATION_PLAN 结论）。

### D6：车载供电方案【已拍板 2026-08-26】

树莓派 5 由小车 24 V 电池经 DC-DC（24 V→5 V / ≥3 A，留足电流裕量）取电，实测压降正常、运行稳定，不再依赖外接 5 V 电池组。固定方式：3M 双面胶 + 尼龙扎带；雷达与树莓派共用 24 V 总线但各自稳压。USB 设备（C50C、X3 Pro）均经 udev 固定设备名，禁止依赖 `ttyUSB0/1` 顺序。

---

## 11. 测试数据与可追溯性要求

每次正式测试至少保存：Git 提交号、OS/ROS/依赖版本、全部 YAML/launch 参数、原始任务 JSON 与转换后路径、原始传感器数据（rosbag）、RTK 解状态/卫星数/差分龄期、规划路径与实驶轨迹、异常事件、场地/天气/地面/载荷/操作者、开始结束时间与成败原因。

录包话题（以实际名称为准）：`/tf`、`/tf_static`、`/scan`、`/imu/data_raw`、`/imu/data_calibrated`、`/odom`、`/odometry/filtered`、`/fix`、`/cmd_vel`、`/validation/*`、`/mission/status`、`/battery_state`、`/diagnostics`。

独立真值优先方案：车顶 ArUco/AprilTag + 高处固定相机 + 场地角点单应性标定；最低限度也要卷尺基准点 + 俯视视频人工复核。**不允许只用机器人自身定位评估自身精度**。

不要只保留截图，图表必须能从原始数据重新生成。测试结果放 `tests/results/`（敏感数据不入公开仓库）。

### 11.1 已执行测试记录（按时间倒序）

> 每次正式测试在此追加一行，与上节要求呼应。原始数据/日志路径必须可查。

| 日期 | 测试项 | 条件/场地 | 结果 | 原始数据位置 |
|---|---|---|---|---|
| 2026-08-31 | **canonical 模拟农田重复地图一致性**（两次同配置固定低速闭环） | 本地 ROS 2 Humble/Gazebo Classic；同一 8×6 m 场景、正式收紧回环参数、无 GUI、`ROS_DOMAIN_ID=42`；不连接树莓派/串口/实车 | **通过（仿真重复性）**：两次地图均通过三垄几何门；对应垄中心偏移均 0.050 m，限值 0.150 m；两次覆盖率分别为 100%/100%/99% 与 100%/100%/100% | 比较命令 `spore-patrol-robot-feature-lidar/tools/compare_farmland_maps.py`；输入 `tests/results/20260831_133835_sim_farmland_8x6/` 与 `tests/results/20260831_135053_sim_farmland_8x6/` |
| 2026-08-31 | **canonical 模拟农田首轮建图**（Gazebo + SLAM Toolbox + 固定闭环） | 开发电脑本地 ROS 2 Humble/Gazebo Classic；8×6 m、3 条垄、1.20 m 净通道、1.60 m 地头；无 GUI；`ROS_DOMAIN_ID=42`；不连接树莓派/串口/实车 | **通过（仿真首轮）**：`/scan`、`/odom`、`/map` 启动可用；9 个 `/odom` 反馈航点全部到达；YAML/PGM 与 pose graph/data 保存成功；通用栅格验收通过（约 7.85×5.85 m、0.05 m/cell）；三垄几何验收通过（覆盖率 100%/100%/100%，最大轨迹离散约 0.030 m）；正式回环阈值已收紧，旧宽松配置的弯曲地图判为失败 | `spore-patrol-robot-feature-lidar/tests/results/20260831_135053_sim_farmland_8x6/`（含 `simulation.log`、`trajectory.log`、`map_validation.log`、`farmland_map_validation.log`、地图与 pose graph/data）；脚本 `tools/sim_mapping_smoke.sh` |
| 2026-08-30 | **车端基础栈恢复与重启演练**（底盘 + 雷达 + EKF + SLAM + rosbridge） | 树莓派在线，小车静止；仅软件重启/容器重启，不启动路线跟踪 | **通过**：SSH/Ping 正常；`/odom`、`/imu/data_raw`、`/odometry/filtered` 约 20 Hz，`/scan` 约 11.3 Hz；底盘反馈约 20 Hz、电池约 23.7 V；TF 链完整；电脑端 WebSocket 收到融合里程计、雷达、电池、诊断；重启后保持单实例；`/cmd_vel` 发布者数=0 | 本次终端/系统日志：Pi `journalctl -u spore-vehicle-stack.service`、`journalctl -u spore-rosbridge.service`；车端服务脚本见 `spore-vehicle-nav/deploy/` |
| 2026-08-26 | **阶段 C 基础组合栈启动检查**（底盘 + 雷达 + EKF + SLAM Toolbox） | 树莓派上车联调 | **基础链路通过**：`/odometry/filtered` 约 20 Hz、`/scan` 约 11.45 Hz，`map→odom→base_footprint` TF 链存在；正式地图尚未保存，因此不作为阶段 C 完成证明 | `spore-vehicle-nav/results/pi_logs_20260826/`；§15 v0.6.3 |
| 2026-09-01 | **单垄(5箱×3m)场地试验：建图链路验证 + U形掉头** | 临时场地(5纸箱单垄)，人在场急停，0.08~0.10 m/s，障碍停车关闭 | **建图链路跑通**：车自主行走1.73m→地图占用格4210→5160增长→map_saver生成PGM/YAML + serialize_map生成posegraph/data双格式全部落盘并拉回存档。**U形掉头成功**：去程3.36m航向漂移仅0.2°；地头180°掉头yaw−139°→+69°，向左侧(远离垄)转弯无碰撞。遗留：路线全长12.8m需≈160s，首次时限130s被截断于终点前0.24m(容差内)，未跑到TASK_FINISHED | `spore-vehicle-nav/results/verify_map_20260901/`(地图+位姿图12MB)、`pi_logs_20260901/`(37个日志) |
| 2026-08-26 | **阶段D分级试验收官**：方形回环(1.5m边×4转弯)、完整多点采样路线(5航点2U形转)、静态障碍停车演示、双垄/直线复测 | 空旷地面(热点点对点)，急停就绪，0.12 m/s | **全部通过**：回环闭合误差0.22m；完整路线FSM全序列+TASK_FINISHED；障碍停车"行驶→刹停→锁定→移障恢复"完整证据链(obstacle_stop时间线 8false/8true/54false)；新增节点能力：/scan前向±90°扇区近距停车(修复QoS不兼容+BEST_EFFORT订阅、排除车尾数据线自回波误触发) | `spore-vehicle-nav/results/trial3_*.json`、`trial6_*.json`；Pi容器/ws/results/r*_status.log |
| 2026-08-26 | 阶段D分级试验②⑤：3m直线 + 双垄往复(垄长3m×2、间距1.0m、U形转弯、末端采样S2) | 空旷地面，急停就绪，0.12 m/s | **均通过**：②位移2.92m/3.00m端点差0.08m；⑤6航点FSM全序列实车走通(含U形转弯+SAMPLING 2s驻留+TASK_FINISHED)，终点误差~0.18m≤容差0.25m、末端航向吻合(~5°)；初始段横向偏差为Pure Pursuit正常收敛瞬态后自动归线 | `spore-vehicle-nav/results/trial2_*.json`、`trial5_*.json`；事件transcript在Pi容器/tmp/t5_status.log |
| 2026-08-26 | **阶段 D 地面 1m 直线首测**（`route_tracker`，从架空冒烟转落地） | 空旷地面，急停就绪，0.12 m/s | **通过（首次实车）**：车实际移动约 0.92 m（目标 1 m，采样容差 0.15 m 内）；`/cmd_vel` 10 Hz 持续发布（满足 0.30 s 看门狗）；`/mission/status` 1 Hz 心跳 + 状态 JSON 正常；路线解析 3 航点 → 11 平滑点；规划→变换→Pure Pursuit→底盘→里程计闭环打通 | `spore-vehicle-nav/results/`（sample_route.json、stage_d_*.sh；Pi 上日志 /tmp/ground_*.log） |
| 2026-08-20 | failsafe 失联停车（复测 2 次） | PC 连小车，车轮架空，急停就绪 | **FAIL（严格判定）**：1.309s / 1.409s 均超 1.2s 阈值；视频确认车轮最终停稳，但固件失联响应偏慢 | `tests/results/20260820_132853_032977_failsafe`, `20260820_133812_492225_failsafe` |
| 2026-08-15 | 底盘 8 小时无人值守稳定性（只读+架空运动+回零） | PC 连小车 USB 直连，车轮架空 | **通过**：48 轮全成功，20Hz 帧流无中断，无异常 stop，主控/watchdog 自然结束 | `PCR/night_run_20260814/`（main/loop_listen/loop_motion/loop_stop/loop_stop_verify.log） |
| 2026-08-15 | 架空低速运动（单轮预验证） | 同上 | 通过：vx=0.05 m/s×2s，运动后回零 | `PCR/night_run_20260814/loop_motion.log` |
| 2026-08-15 | 真实串口只读监听 | PC 连小车 | 通过：20Hz 状态帧、24.1V | `PCR/night_run_20260814/real_listen.log` |
| 2026-08-15 | 离线协议单测 + fake_stm32 模拟器（5 轮） | 不依赖小车 | 通过：单测 1 用例 OK，模拟监听 5/5 | `PCR/night_run_20260814/unittest.log, fake_round*.log` |
| 2026-08-14 | 树莓派部署（24.04.4+Docker Humble，8 包编译） | arm64 实机 | 通过：SSH、Docker、SDK、colcon 全成功 | `树莓派5部署清单.md §0-1` |
| 2026-07-26/27 | 底盘驱动、3m 直线、旋转、失联停车 | 实车（PC） | 通过（团队前期记录） | 仓库 README / dev_log / tests/results |

---

## 12. 风险清单（2026-08-30 更新）

| 风险 | 影响 | 当前应对 |
|---|---|---|
| RTK 采购物流延误（关键路径） | 阶段 E 整体后移，精度指标无证据 | 今天启动选型下单；准备仅 SLAM+里程计的降级演示 |
| CORS/NTRIP 不可用或收费 | RTK 固定解链路断裂 | 选型前确认；备选自建基站方案列入预算评估 |
| 树莓派 arm64 编译/性能问题 | 部署延误 | 先验证 ydlidar SDK 等最重依赖；备选 PC 车载过渡 |
| 树莓派车载供电不稳 | 运行中断、数据损坏 | 【已缓解 2026-08-26】DC-DC 24V→5V 已上车实测压降正常、Pi 5 稳定；保留压降复测作为上车前例行检查 |
| **固件失联停车响应偏慢** | 真实失联时车辆刹车滞后（实测 1.3~1.4s，验收阈值 1.2s） | 【2026-08-20 实测 FAIL，2026-08-26 仍未处理】固件 D50A 超时机制未排查；ROS 驱动层看门狗（cmd_vel_timeout_s=0.30）为实际使用兜底机制，待验证；限速运行、急停就绪兜底 |
| X3 Pro 室外强光/植株退化 | 建图与避障失效 | 到货后尽快室外测试；轮速+IMU+RTK 冗余；限速 |
| 桌面/Web 两套 JSON 分裂 | 车端重复开发 | 决策 D1 冻结为 route_v1.json + 离线转换 |
| 采样器接口未定 | 阶段 F 延误 | 模拟节点先行，接口字段尽早冻结 |
| 续航表述不当 | 评审扣分或被认为造假 | 实测数据 + 方案说明（决策 D4） |
| 时间不足 | 闭环不完整 | 最小闭环优先（路径执行+停车+回传），功能逐步加 |
| **阶段 C/D 证据未收口** | 阶段 C canonical 仿真已产生通过结构/几何验收的地图，但仍缺室内/小花园实机地图、重复路线 rosbag；阶段 D 尚无全等级×3、独立真值 P95 和完整专项记录 | 以已收紧回环阈值的仿真基线作为软件回归入口，再完成小花园建图与路线取证；同步补齐独立真值、重复/暂停/看门狗证据；阶段 E 仍需完成 RTK 选型决策 |
| 提交邮箱拼写错误 | 材料无法送达 | 提交前与官方联系人核实（白老师 <PRIVATE_PHONE>） |
| 校园 WiFi 禁组播，DDS 发现失败 | PC 与 Pi 话题互相看不见 | 统一 ROS_DOMAIN_ID；改手机热点/网线；必要时 CycloneDDS 静态对端配置 |
| 小花园场地受限/被干扰 | 模拟农田测试中断 | 提前报备占用时段；场地布置可快速拆装；备选校园空地 |
| 公开仓库泄露敏感信息 | 安全/合规 | 遵守协作说明第九节；坐标、账号、凭据不入库 |

---

## 13. 交给 agent 的工作规则

将本文件交给 agent 后，首先让其完整阅读本文件、`INTEGRATION_PLAN.md`、`NAVIGATION_VALIDATION_PLAN.md` 与目标仓库 README。后续工作遵守：

1. 先检查现有代码、驱动和未提交改动，不覆盖已有成果（尤其 `feature/miobarnacle` 与 feature-lidar 分支内容）。
2. 所有硬件控制前确认安全条件：先架空轮、急停在手、先只读后运动、失联停车已验证。
3. 技术栈事实以本文档 §3/§4 为准：ROS 2 Humble、R550 PLUS（C50C 串口协议）、X3 Pro、route_v1.json；不要再走 v0.1 的"接口未知盘点"老路。
4. 不重写现有路径规划器与底盘驱动，优先适配层接入。
5. 每个阶段先定义输入、输出和完成判据再写代码；每次只解决一个可验证的问题并保存证据。
6. 采购前列出接口、必要参数、预算和备选方案。
7. 外部设备接口必须支持超时、重试、取消和故障码。
8. 实测、仿真和目标值分别标注；公开仓库不放敏感信息。
9. 每次任务结束后更新本文档状态与第 15 节更新记录。

建议首次指令模板：

```text
请完整阅读《机器人车辆导航子系统工作计划与agent协作指南.md》（v0.2）。
当前执行【阶段 X】的第 N 项任务。先复述你对输入、输出和完成判据的理解，
确认无误后再动手。完成后给出证据（命令、日志、文件路径），
并同步更新计划文档对应状态与更新记录。
```

---

## 14. 尚待回答的问题（2026-08-14）

- ~~树莓派具体型号、内存、存储？~~【已确认 2026-08-14：树莓派 5，8 GB，64 GB 存储卡，电池供电可用】遗留：集成阶段改为小车 24 V→5 V DC-DC 取电
- ~~7 月联调 X3 Pro 归属 / 自购雷达到货时间 / 是否含原装转接板线缆？~~【已全部确认并落地 2026-08-26：自购新机已到货，2026-08-20 完成阶段 B 复验，含原装转接板与线缆，udev 固定名 `/dev/ydlidar` 已建】
- ~~预算是否包含雷达？~~【已确认：3000 元专项用于 RTK 链路】CORS/NTRIP 账号有无、费用——见附录 A 问询
- ~~测试场地？~~【已确认暂定 2026-08-14：学校小花园轻量化模拟验证，不进真实农田；试验基地保留为可选加分项】遗留：小花园占用许可与道具报备
- 项目组对续航指标（8 h 示例值）的表述决策（决策 D4）？
- 采样器负责人能提供什么通信接口？何时可联调？
- 决策 D1（route_v1.json 作为车端标准）是否确认？桌面规划工具后续是否增加同款导出？
- 团队名称与联系人电话（提交命名规范需要）？
- 提交邮箱拼写以哪个为准（jnxiangchen@ / jnxianchen@）？

---

## 15. 更新记录

| 版本 | 日期 | 更新内容 |
|---|---|---|
| v0.1 | 2026-08-13 | 初版：竞赛要求、职责范围、硬件假设、总体架构、阶段计划、安全盘点流程和协作规则 |
| v0.2 | 2026-08-14 | 全面对照项目实际盘点后重写：更正 R680→R550 PLUS 等 7 项事实（§4）；从比赛 PDF 原文提取要求（§2）；登记全部软件资产与已验证能力（§3）；按现状重排阶段计划 A–G 与 32 天倒排表（§7/§8）；明确 RTK 采购为关键路径；更新决策门、风险与待答问题 |
| v0.2.1 | 2026-08-14 | 用户确认：主控为树莓派 5（8 GB）；7 月联调雷达在其他成员手中，自购 X3 Pro 预计 2026-08-18 到货（与阶段 B 起始日期一致）。同步更新 §3.1、§4、§9、§10-D2、§14 |
| v0.2.2 | 2026-08-14 | 用户确认：64 GB 存储卡、电池供电可用、3000 元预算不含雷达（专项用于 RTK）、任务包格式同意。**决策 D1 冻结（route_v1.json）**；新增附录 A 北斗 RTK 问询清单；同步更新 §3.1、§5.4、§9、§10-D1/D3、§14 |
| v0.2.3 | 2026-08-14 | 用户确认测试场地=学校小花园轻量化模拟验证。**决策 D2 拍板**：Ubuntu Server 22.04（arm64）+ 原生 ROS 2 Humble（更正 v0.2 误写 24.04：Humble 不支持 24.04）；阶段 A 转入【进行中】，新建《树莓派5部署清单.md》操作手册（命令已按仓库实际入口核对）；阶段 C/G 与风险清单同步场地调整；§14 场地问题关闭 |
| v0.3.0 | 2026-08-14 晚 | **阶段 A 软件部署完成**（agent 远程执行）：22.04 镜像不支持 Pi 5 → D2 再修订为 24.04.4 + Docker Humble 容器；修复镜像 SSH 0 字节主机密钥 bug；树莓派上线 <VEHICLE_HOST>（热点 <PRIVATE_IP>）；Docker/ros-base/依赖/SDK 就绪，8 包编译通过。待办：底盘 USB 连接与闭环测试。详见部署清单 §0-1 |
| v0.3.1 | 2026-08-15 凌晨 | **夜间自动测试（8小时，架空确认）**：小车 USB 接电脑（CH340→/dev/ttyACM0），只读 20Hz/24.1V 正常；架空低速运动 vx=0.05m/s×2s 通过（`--arm WHEELS_OFF_GROUND` 安全参数）；`night_loop.sh`（96→48轮，600s间隔）+ `night_watchdog.sh`（崩溃自愈一次）后台运行；日志在 `night_run_20260814/`。待办：failsafe 失联停车（需人在场+急停就绪） |
| v0.4.0 | 2026-08-15 上午 | **夜间测试复盘并归档**：48 轮/8 小时全通过（只读 48/48、架空运动 48/48、回零 48/48、异常 stop 0 次、主控自然结束）；新增 §11.1 已执行测试记录表；§3.3 补充 PC 端 8 小时实测能力；阶段 A 勾选更新；剩余待办：failsafe 失联停车（白天人在场）与 PC 端 RViz 联调 |
| v0.5.0 | 2026-08-20 | **中期恢复+阶段 A 收尾**：08-15~08-20 暂停（用户临时安排）；failsafe 失联停车复测 2 次均 FAIL（1.309s/1.409s 超 1.2s 阈值，视频确认车轮停稳，固件响应偏慢，风险已登记）；提取并初步分析 C50C 固件源码（D50A 无刷驱动、FlagStop 字段）；更新 §11.1/§12。下一步：阶段 B 雷达到货验证 |
| v0.6.0 | 2026-08-26 | **状态校准与章节修订**：据用户确认——雷达已到货并完成阶段 B 复验、车载 DC-DC 供电已落地（新增决策 D6）、硬件第一批采购完成；但**阶段 C（建图）与阶段 D（路径跟踪）截至今日均未启动，较 §8 倒排表滑期**；failsafe 仍 FAIL 未处理；RTK 仍搁置。同步修订 §3.1/§3.4/§7/§8(进度预警)/§9/§10(D6)/§12/§14，剩余约 20 天。下一步：重启阶段 C 建图 |
| v0.6.1 | 2026-08-26 | **新增代码归集仓库**：创建 `spore-vehicle-nav/`（colcon 工作区 + 本地 git），作为阶段 C/D 车端代码与后续成果归集目录；写入 `CONVENTIONS.md` 统一坐标系/TF/话题/速度约束；阶段 D 包（路径跟踪）与阶段 C 启动配置（EKF+SLAM）由 agent 并行实现中（见 §3.2）。本文件晋升为车辆侧主审计文档并索引该代码仓 |
| v0.6.2 | 2026-08-26 | **阶段 C/D 车端代码落地并离线验证**：`spore_patrol_route_validation`（route_v1.json 解析+校验、field→map 变换、路径平滑/重采样、Pure Pursuit、采样任务状态机、ROS 节点）**82 项 pytest 全通过**；`spore_patrol_nav_bringup`（robot_localization EKF 融合 /odom+/imu/data_raw→/odometry/filtered、SLAM Toolbox mapping/localization 切换启动、TF 归属正确）**YAML 校验通过**。另草拟 `RUNBOOK.md` 上车运行手册。代码已就绪，待树莓派 colcon build 与上车实车验证。failsafe/RTK 仍维持原风险（§12）。 |
| v0.6.3 | 2026-08-26 | **阶段 C/D 上车实测（首轮）**：SSH 直连 Pi（免密，<PRIVATE_IP>）；修复两个包 ament 可执行文件安装（根因：缺 `setup.cfg` 的 install_scripts、buildtool 误用 ament_cmake → 脚本落到 bin/ 而非 lib/\<pkg\>/，已修）；确认 Pi 上 EKF/SLAM/底盘/雷达全栈在跑（/odometry/filtered 20Hz、/scan 11.45Hz、TF 链 map→odom→base_footprint 完整）；Stage D 架空冒烟 → 地面 1m 直线实测：车移动约 0.92m，/cmd_vel 10Hz、/mission/status 1Hz 心跳正常；节点新增状态心跳（生产改进，解决 DDS 首帧丢失）。待办：完整牛耕式路线分级试验、状态事件全序列记录、采样联动（§11.1 已记录）。 |
| v0.6.4 | 2026-08-26 | **分级试验②⑤实车通过**：3m直线(端点差0.08m)+双垄往复(6航点全序列、U形转弯、SAMPLING驻留、终点差0.18m)；确认初始段横偏为Pure Pursuit收敛瞬态属正常自动纠偏；正式P95误差仍需独立真值测量(§11要求)。剩余分级：每级×3重复、90°方形回环、静态障碍停车；小花园场地后正式建图+落盘保存。RTK采购期限09-03不变。 |
| v0.6.5 | 2026-08-26 | **阶段 D 分级试验全部收官（当日 6 类 9 次全通过）**：①1m直线②3m直线×3③方形回环④静态障碍停车⑤双垄往复×2⑥完整多点采样路线。关键产出：a) 障碍停车能力上线——/scan 订阅改 SensorDataQoS(修 BEST_EFFORT 不兼容)、判据改前向±90°扇区(排除车尾数据线自回波，用户确认前向180°净空)；b) "行驶→刹停→锁定→移障自动恢复"完整证据链入档；c) 协作流程确立：每项试验前公告摆车要求→用户重摆→重新读取位姿→执行→验证。剩余：正式报告用 P95 横向误差需独立真值(俯视相机/卷尺基准)、小花园场地建图落盘、每级×3重复补满、RTK 选型采购(09-03)、failsafe 固件排查、采样器接口冻结。 |
| v0.7.0 | 2026-08-27 | **计划重排至收尾三窗口**：阶段 D 判定"小尺度全部通过"，正式证据转移至小花园（建图落盘 + 牛耕全程 + 独立真值）；§9 重写为窗口一(P0: RTK 下单 09-03 / 花园建图落盘)/窗口二(精度取证 + 采样器接口冻结)/窗口三(演示视频 + 报告材料)；新增风险：RTK 物流倒逼、CORS 自建成本决策上交组长、雨天回退当前场地录素材。版本管理现状：私有仓 miobarnacle-ops/spore-vehicle-nav main=db3abfa，含 HANDOVER 接手清单（热点/Pi 登录方式/红线/踩坑表），团队随时可接管。 |
| v0.6.6 | 2026-09-01 | **单垄场地试验日（建图能力验证 + U形掉头）**：a) 建图链路端到端跑通并取证——车自主走1.73m、地图占用格增长、map_saver(PGM/YAML)+serialize_map(posegraph/data)双格式落盘并拉回 `results/verify_map_20260901/`；b) U形掉头成功——去程3.36m航向漂移0.2°、地头180°转弯向远离垄侧完成无碰撞；c) 收获三条硬教训：①**贴垄作业必须关闭障碍停车**（0.5m阈值与0.6m墙距天然冲突，会锁死）；②**掉头必须向远离垄的一侧转**（首次默认向右转即撞箱，改为向左转成功）；③**路线时限按「总长÷速度×1.4」留足**（12.8m@0.08~0.10需≈160s，给130s被截断）。另发现：route_tracker 被 timeout 杀后残留 `<defunct>` 僵尸进程（无害，重启容器可清，但 pgrep 会误报"仍在运行"）。 |
| v0.7.1 | 2026-08-30 | **远程无实机 W0/W1 完成**：冻结本地 Web 控制台（`spore-monitor-web@dd6b6dc`）、车端代码（`spore-vehicle-nav@623932c`）和四份 Python 软著参考源的哈希；完成源码级 Python→Web 功能对照，确认控制台作为后续整合基础，旧“穗巡”空壳平台不再作为任何实现基线。Web lint/类型检查/构建及 22 项测试、车端 83 项 pytest 和 YAML 校验均通过；结果仅为离线验证，未连接实机或发布线上站点。详见 `远程无实机基线清单.md` 与 `spore-monitor-web/docs/PYTHON_WEB_PARITY_AUDIT.md`。 |
| v0.7.2 | 2026-08-30 | **Web 整合原则澄清**：Python 软著是 Web 控制台的功能来源与可追溯参考，不是要求逐项复刻的唯一实现标准。后续以 `spore-monitor-web` 为演进基础：守住明确继承的业务语义和冻结接口，按需补齐控制台接口及集成能力；新增/增强功能必须单列边界、测试和证据，不伪装为软著原能力。W2 调整为“核心算法兼容性回归与增强边界”。 |
| v0.7.3 | 2026-08-30 | **远程无实机 W2 完成**：新增软著核心兼容性夹具与 `npm run test:parity`（9 项通过），覆盖几何、规划语义、风险/稳定度、PCR 定量/判定/离群、Pearson 比较和 3/5/7 天预报输出；将 Ct 离群规则下沉至计算层；Web 明确保留多边形重心缺陷修正、Gaussian plume/预报增强和规划输入前置校验。未改变已冻结 `route_v1.json` / `schema_version: "1.0"` 接口，未连接实机。详见 `spore-monitor-web/docs/WEB_ENHANCEMENT_BOUNDARIES.md`。 |
| v0.7.4 | 2026-08-30 | **远程无实机 W3 完成**：冻结 Web—车辆 `1.0` 集成契约，规范任务状态/事件、位姿/雷达/电池/诊断/RTK/采样器遥测、关联键和新鲜度；新增 Web 与车端各自的纯逻辑校验，并共用正常、缺字段和未知状态 fixtures。路线接口保持 `route_v1.json` / `schema_version: "1.0"` 不变；Web 2 项契约测试、路线 4 项测试及车端 85 项 pytest 均通过。详见 `spore-monitor-web/docs/INTEGRATION_CONTRACTS.md`。 |
| v0.7.5 | 2026-08-30 | **远程无实机 W4 完成**：实现本地 `mock/replay/rosbridge` 数据总线和确定性任务模拟器；可覆盖加载、行驶、到点、采样、返航与完成，并支持断联、位姿异常、RTK 降级、低电量、雷达超时/障碍、采样器忙/失败/超时等故障注入。新增版本化回放 fixture 和 3 项数据总线回归；无实机模式不发布真实 `/cmd_vel`。详见 `spore-monitor-web/docs/MOCK_REPLAY_GUIDE.md`。 |
| v0.7.6 | 2026-08-30 | **远程无实机 W5 完成**：在既有 Web 控制台业务页签中接入统一任务/遥测面板，路径规划可展示并传递 `route_v1.json` 任务包摘要；Mock/Replay/rosbridge 同屏显示任务、RTK、采样器、诊断、事件和 `sample_id` 关联，Mock 控制仅改变本地模拟器。rosbridge 改为显式连接；保留“软件停车不等于物理急停”说明。Web 完整离线质量门通过，未连接实机或发布线上站点。 |
| v0.7.7 | 2026-08-30 | **远程无实机 W6 完成**：新增版本化本地会话仓库，统一封装浏览器存储、工作区自动恢复、导入导出和 `version: 5 → schema_version: "6.0"` 迁移；会话记录用户录入/模拟/回放/派生数据来源，不保存密码、令牌或设备凭据。未知版本、损坏结构和敏感键会被拒绝且不覆盖既有工作区；新增 2 项仓库回归通过。 |
| v0.7.8 | 2026-08-30 | **远程无实机 W7 完成**：车辆端路线解析器新增航点序号连续性与采样 `sample_id` 拒绝规则；状态机补齐暂停/继续、返航、取消的显式离线转移。将里程计/雷达超时、前向障碍和未知任务状态的停车判定抽为纯函数并接入 ROS 控制循环，缺失或过期输入按 fail-safe 输出零速度；既有 `results/` 路线成为只读回归输入。`verify.sh` 通过 91 项 pytest 和 YAML 校验；仅为代码/模拟验证，未连接实车。 |
| v0.7.9 | 2026-08-30 | **远程无实机 W8 完成**：新增根目录 `verify_remote.sh` 统一执行 Web 格式/类型、兼容性、契约、数据总线、持久化、演示、路线、引擎、构建/渲染测试与车辆 `verify.sh`，成功后生成分栏的离线质量门摘要。离线演示清单覆盖正常回放、RTK 降级、低电量、障碍停车、采样失败、断联恢复；本次质量门全通过，未访问外网或连接 ROS/串口/实机。 |
| v0.8.0 | 2026-08-30 | **远程无实机 W9 完成，远程计划收口**：新增 `spore-vehicle-nav/docs/实机接入前置包.md`，冻结 ROS_DOMAIN_ID、rosbridge 占位地址、话题/消息/频率/超时表、先只读后低速的现场顺序、RTK/采样器/相机最小契约样例与证据模板；未保存设备凭据、未连接实机。远程代码/模拟结果与实机性能、安全、精度验证明确分离。 |
| v0.8.1 | 2026-08-30 | **附录 A 回复入档并完成 ATGM336H ROS 2 离线适配包**：确认已购 ATGM336H（¥236）为单频单点 GNSS，不支持 RTK Float/Fix、RTCM 差分或双天线定向；已完成 `/dev/ttyUSB0`、9600 baud、NMEA 的 ROS 2 包、GGA/GLL/VTG 解析、`/fix`/速度/原始数据/诊断、可选 WGS84→ENU、launch/YAML 与 5 项离线 GNSS 测试。普通定位链路可继续联调，但 `<0.5 m` 仍须 RTK/独立真值实测，不得由该模块或融合算法直接宣称达成。 |
| v0.8.2 | 2026-08-30 | **阶段 C/D 状态同步**：将阶段 C“基础组合栈已在树莓派启动检查”与“正式地图/模拟农田建图/rosbag 未完成”拆分记录并勾选对应子项；将阶段 D 路线包更新为当前 91 项离线 pytest，通过记录的小尺度 6 类 9 次实车结果同步至清单；保留每级×3、重复 5 次、暂停/看门狗专项、独立真值 P95 和小花园正式路线等未完成项。同步修正当前状态摘要中的过时“82 项/未启动/待上车”表述，历史版本日志保留对应时间点的原始记录；不改变 RTK 未决和无实机远程验证边界。 |
| v0.8.3 | 2026-08-30 | **实机恢复与服务化修复**：确认电脑 `<PRIVATE_IP>` ↔ 树莓派 `<PRIVATE_IP>` 的 SSH/网络链路；修复 ROS 2 Humble 下 `FindPackageShare` 导入错误、容器内 `/dev/ydlidar` 别名缺失和重启后 ROS 子进程残留；新增 `full_stack.launch.py`、车端 `spore-vehicle-stack.service` 与 `spore-rosbridge.service`，rosbridge 监听 9091。静止实测 `/odom`、`/imu/data_raw`、`/odometry/filtered` 约 20 Hz，`/scan` 约 11.3 Hz，TF/电池/诊断正常；电脑端真实 WebSocket 收到四类数据，容器重启后单实例自动恢复；Web 默认地址和融合里程计优先级同步修复。未启动路线跟踪或非零 `/cmd_vel`，正式地图、运动/精度/failsafe/RTK/采样器验收仍按未完成项保留。 |
| v0.8.4 | 2026-08-30 | **远程软件联调继续推进**：新增 Web `rosbridge-data-adapter`，将车端原生 `/mission/status` 大写状态映射为统一 `mission_status/mission_event` 契约，接入可选 `/gps/status` 并对位姿、雷达、诊断执行新鲜度和前向障碍计算；新增 4 项适配回归测试（契约测试合计 6 项）；将 `spore_patrol_gnss` 部署至树莓派，`enabled=false` 启动检查通过。未启动路线跟踪、未发布非零 `/cmd_vel`；GNSS 实串口、采样器/视频和比赛级运动/精度验收仍待硬件与场地条件。 |
| v0.8.5 | 2026-08-31 | **阶段 C 仿真基线落地**：审计发现旧 `farmland.world` 为 24×14 m 占位场景，与正式 8×6 m 验证约束不一致；在当前雷达/SLAM 分支新增 `farmland_8x6.world`、`farmland_layout.json` 和仿真专用 SLAM 参数，固定 3 垄/1.20 m 净通道/1.60 m 地头/0.35 m 激光高度/无障碍首轮；新增 `validate_farmland_layout.py`、`validate_saved_map.py`、`sim_mapping_smoke.sh` 和 6 项离线回归，SDF 检查、包拓扑和脚本语法均通过。因开发电脑无可直接调用的 ROS 2，Gazebo/SLAM 实际运行和地图文件仍保持未完成，不把离线通过写成建图完成。 |
| v0.8.6 | 2026-08-31 | **阶段 C 仿真首轮建图完成**：在独立 ROS 域 `42` 中实际运行 ROS 2 Humble + Gazebo Classic + SLAM Toolbox；无 GUI 启动、`/scan`/`/odom`/`/map`、9 航点固定低速闭环、map_saver、pose graph/data 保存和通用地图验收均通过。新增 `validate_farmland_map.py` 与 2 项回归，对三垄覆盖/偏移/离散做自动几何门；发现并修复正式配置中宽松回环阈值造成的弯曲重影，收紧后的正式配置地图通过三垄几何检查。实机/小花园地图、重复路线 rosbag、独立真值和 RTK 精度仍未完成；未连接树莓派、串口或真实小车。 |

---

## 附录 A：北斗 RTK 问询清单（发给负责成员）

> 目的：本人需要在 3000 元预算内把 GNSS/RTK 接入树莓派并评估"定位误差 <0.5 m"比赛指标。以下问题按优先级排列；已收到成员对 A1–A4 的阶段性回复，现将事实与未决项记录如下。

### A1. 硬件现状（最高优先级，直接影响排期）

1. RTK 接收机/开发板是否已选型或已购买？型号是什么？价格？已购的话预计到货/到手时间？
2. 该接收机支持哪些卫星系统和频点？（需要：北斗 BDS 至少双频，如 B1I+B2I 或 B1C+B2a；兼容 GPS/GLONASS/Galileo 更好）
3. 标称定位精度：单点解、RTK 浮点解（Float）、RTK 固定解（Fix）分别多少？
4. 物理接口和输出协议：串口/USB/TTL？波特率？输出 NMEA-0183 吗？支持 RTCM 差分输入吗（版本 2.3/3.x）？
5. 输出频率：1 Hz / 5 Hz / 10 Hz？（车辆巡检 1–5 Hz 即可）
6. 天线情况：是否配测量型天线？馈线长度？是否磁吸/螺栓安装？增益多少？
7. 是否支持双天线定向（航向角）？如果不支持，车辆航向将由北斗速度方向/融合定位解决，需注明。
8. 是否有 PPS 秒脉冲输出？（多传感器时间同步可能用到）

#### A1–A4 成员阶段性回复（2026-08-30）

| 项目 | 已确认事实 | 对项目的影响 |
|---|---|---|
| 硬件 | 已购买 ATGM336H，价格 ¥236；Ubuntu 串口识别为 `/dev/ttyUSB0`，9600 baud | 普通 GNSS 链路已有硬件基础；预算尚余约 ¥2764，但该模块不是 RTK 接收机 |
| 卫星与精度 | 单频多模，支持北斗/GPS 等；不支持双频 BDS、RTK Float、RTK Fix；标称为米级单点定位 | 不能直接满足或证明比赛 `<0.5 m` 指标 |
| 接口与输出 | 已读到 `$GNGGA`、`$GNGLL`、`$GNVTG` 等标准 NMEA；RTCM 2.3/3.x 未支持/未测试；配套普通 GNSS 天线，不是 RTK 测量型天线；不支持双天线定向 | 可做 NMEA→ROS 2 适配和普通 GNSS 降级/初始位置源；不能形成 RTK 固定解或双天线航向 |
| 频率 | 默认 1 Hz，最高通常 10 Hz | 车辆软件联调前需实机核对实际输出频率与时间戳 |
| ROS 接入 | 已用 `nmea_navsat_driver` 完成 ROS 1 Noetic 接入并发布 `/fix`；室外已获得经纬度 | 证明协议和硬件初步可用；车端主线为 ROS 2 Humble，仍需 ROS 2 适配 |
| 实测样例 | `latitude=40.00063316666667`、`longitude=116.35406633333334`、`altitude=51.1` | 这是一次定位读数，不是误差统计；尚缺原始 NMEA 文件和独立真值 |
| 差分与坐标 | CORS/NTRIP、Caster、账号、RTCM 链路均未调查；经纬高→ENU 尚未开发 | RTK 全局融合尚未启动；可先完成 WGS84→ENU 软件适配，原点须由现场测量确定 |
| 协作状态 | 普通 GNSS 可进行车辆侧软件联调；模块已花费 ¥236 | 可立即开展 ROS 2 离线包和随后串口冒烟；`<0.5 m` 仍是待实测/待硬件决策事项 |

### A2. 差分链路（决定能否达到固定解）

1. 测试场地/学校所在城市是否有可用 CORS 网络（如省级/市级 CORS、千寻位置、六分科技等服务）？
2. NTRIP 账号：是否已有？若无，开通费用多少（包年/包月）？Caster 地址、端口、挂载点名称是什么？
3. 差分数据的网络接入方式：是否配 4G 模块和 SIM 卡？流量费？还是用手机热点？
4. 若 CORS 不可用：所选接收机能否切换为自建基站模式？自建基站大致成本（需第二台接收机或一套基准站）？

### A3. 软件与数据（决定接入工作量）

1. 接收机是否有 ROS 2 驱动或厂商 Demo？若无，能否确认输出标准 NMEA（可用 `nmea_navsat_driver` 接入）？
2. NMEA 语句中能否读到 RTK 解状态（Single/Float/Fix）？通过哪条语句（如 GST、GGQ 或厂商私有语句）？
3. 有无一段实测 NMEA 样例数据（原始记录最好）？
4. 接收机输出的坐标基准是 WGS84 还是 CGCS2000？是否支持输出 ENU/东北天局部坐标？

### A4. 时间与协作

1. 设备（或预算）何时可以交到车辆侧联调？
2. 3000 元预算目前是否已花出？花在哪？剩余多少？
3. 采样器/整机集成是否也需要 GNSS 授时或位置数据？需要的话提前约定接口。
4. 你这边对接收机安装位置（车顶天线支架）有没有要求或已有设计？

> 阶段性决策：ATGM336H 先作为普通 GNSS 软件联调件，不冒充 RTK；`spore_patrol_gnss` ROS 2 离线包已完成。若比赛材料必须以真实数据支撑 `<0.5 m`，仍需项目组另行拍板多频 RTK、差分服务/基站、测量型天线和独立真值方案。
