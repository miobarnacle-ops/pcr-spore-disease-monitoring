# PCR 项目组内窗口期任务说明

> 用途：用于组内同步下一窗口期的任务认领、工作重点和交付标准。
>
> 基本要求：每项任务都要留下可复用的代码、配置、数据、图表、视频或文稿，并在交付说明中写清运行环境、执行命令、验证结果和未解决问题。公开仓库中不得提交账号、密码、网络密钥、设备私密参数等敏感信息。

## 一、报告撰写

### R-01｜SLAM + RTK + 路径规划总方案

负责梳理项目整体技术路线，明确 SLAM 建图、RTK/北斗定位、路径规划、采样执行和控制台之间的数据流与接口关系。需要形成完整系统架构图，并完成方案章节初稿，重点说明各模块输入输出、坐标系、通信链路和验证边界。

**核心交付：**系统架构图、总体方案章节初稿、模块接口和验证流程说明。

### R-02｜小车路径规划与采样点规划

负责研究农田区域覆盖路径、牛耕式路线、地头转弯和采样点布置方法，明确作业区域边界、行距、采样间隔、航向约束和路径执行顺序。需要补充算法推导、采样策略以及典型规划结果图表。

**核心交付：**路径规划推导、采样点规划策略、路线示意图、关键参数和结果表格。

### R-03｜SLAM 激光雷达建图章节

负责说明激光雷达建图原理、扫描匹配、坐标系与 TF 关系、建图启动流程和地图保存方法，同时整理雷达选型、安装方式、量程/频率/分辨率等关键参数及其对建图效果的影响。

**核心交付：**建图原理章节、传感器选型依据、参数说明、建图流程和地图示例。

### R-04｜RTK 北斗定位章节

负责介绍差分定位和 RTK 定位原理，梳理北斗模块、天线、串口、树莓派和 ROS2 节点之间的硬件连接与数据链路，并说明 ENU 坐标转换、定位状态判断和精度评估方法。

**核心交付：**差分定位原理、硬件连接图、ROS2 数据链路、坐标转换说明和精度评估方法。

### R-05｜总控制台章节

负责整理控制台的软件架构、前后端通信协议、车辆状态和任务数据接口，并补充任务管理、地图显示、轨迹展示、告警状态和数据回放等 UI 设计说明。

**核心交付：**软件架构图、前后端接口/数据结构、主要页面说明和 UI 设计稿或截图。

### R-06｜孢子扩散预测算法章节

负责整理孢子扩散的物理模型、变量定义、参数假设和公式推导，说明观测数据如何进入模型、算法如何计算扩散风险，以及预测结果如何输出并在控制台中展示。

**核心交付：**物理模型公式推导、变量与参数表、算法流程图、输入输出和结果解释说明。

## 二、硬件集成

### H-01｜RTK 与小车硬件系统整合

负责完成 RTK/北斗模块与树莓派、小车及 ROS2 系统的接线、通信配置和坐标标定，采集 RTK、SLAM、里程计等融合定位数据，分析轨迹误差和定位稳定性。目标是形成定位测试报告，融合定位误差不超过 **0.5 m**。

**核心交付：**硬件连接记录、系统配置、标定过程、测试数据、误差统计和结论报告。

### H-02｜模拟农田场景搭建与 SLAM 建图

负责搭建包含农田边界、作物行或障碍物特征的模拟场景，完成激光雷达建图、地图清理、尺度确认和坐标检查。最终导出可供导航使用的标准农田地图文件，例如 `.pgm` 与 `.yaml`，并记录建图参数。

**核心交付：**场景搭建记录、建图过程、场景照片或示意图、标准地图文件和地图质量检查结果。

## 三、算法开发

### A-01｜RTK 与 SLAM 融合定位算法

负责实现 RTK 与 SLAM 的坐标转换、时间同步、数据质量判断和融合定位策略，明确不同传感器异常或失锁时的处理逻辑。需要完成对应 ROS2 节点、launch/config 工程，并输出融合轨迹与单独定位轨迹的对比结果。

**核心交付：**算法源码、ROS2 节点工程、launch/config 文件、定位轨迹对比图和误差分析。

### A-02｜孢子扩散预测算法重构

负责重构现有孢子扩散预测代码，拆分数据处理、物理计算、风险预测和结果输出模块，统一输入输出接口并提升代码可维护性。需要使用统一数据集对比重构前后的计算耗时、预测精度、稳定性和边界情况表现。

**核心交付：**优化后的算法源码、测试数据、效率对比、预测精度分析和回归验证结果。

## 四、成果展示

### D-01｜控制台美化与多端应用封装

负责优化控制台页面布局、颜色、交互和任务状态展示，完善 Mock、数据回放和实机连接入口，并处理不同屏幕尺寸下的显示问题。最终完成 Windows/Linux PC 端安装包和移动端 APK，同时提供安装、启动和常见问题说明。

**核心交付：**优化后的控制台、Windows/Linux 安装包、移动端 APK、构建记录和运行说明。

### D-02｜算法对比评测与数据可视化

负责建立新孢子扩散算法与传统高斯羽烟模型的统一对比实验，使用相同输入数据、边界条件和评价指标，比较预测结果、误差、计算效率和稳定性。最终输出适合报告和答辩展示的对比图表与指标表格。

**核心交付：**实验数据、评价指标定义、新旧算法对比图、指标表格和结论文字。

### D-03｜牛耕式自主导航实跑视频录制

负责录制小车按照牛耕式路线自主行驶的视频，重点展示起步、直线巡航、地头转弯、连续路径执行和区域覆盖效果。视频应保留必要的任务状态、轨迹或参数信息，并标注测试场地和运行条件。

**核心交付：**MP4 视频，内容覆盖转弯、巡航和区域全覆盖能力，并附测试说明。

### D-04｜安全动态避障视频录制

负责录制车辆遇到前方动态障碍物时的安全响应过程，重点展示前方约 **±45°** 范围内的障碍检测，以及距离小于 **0.5 m** 时的自动刹停或绕障行为。测试前必须确认急停可用、人员在场、速度受控，并记录每次测试的触发距离和最终结果。

**核心交付：**MP4 视频，展示动态避障、刹停/绕障过程，并附安全条件和测试结果说明。

## 五、统一交付要求

1. 代码类任务需提交源码、配置、启动命令和最小复现步骤；报告类任务需提交可直接引用的正文、图表和数据来源。
2. 测试类交付要区分离线、仿真、回放、树莓派静止链路和实车测试，不得将离线通过写成正式实车精度结论。
3. 视频和图表要注明测试场景、关键参数、日期、异常情况和结论；失败或未完成项也要保留问题记录。
4. 所有成果统一提交到公开仓库对应目录，敏感运行参数通过私密渠道单独传递。
